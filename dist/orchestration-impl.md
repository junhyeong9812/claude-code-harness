# 구현 오케스트레이션 (Implementation Control Tower)

> 코드를 실제로 작성, 수정, 삭제하는 모든 작업의 제어 문서.
> `orchestration.md`(라우터)에서 이 문서로 분기한다.

---

## 0. 대명제 — 이 문서를 반드시 전부 읽어라

**이 문서는 세션 시작 시 전체를 빠짐없이 읽는다.** 토큰이 많이 들더라도 예외 없다.

- 토큰 제한으로 한 번에 읽지 못하면, offset/limit를 사용해 끝까지 나눠서 읽는다.
- 일부만 읽고 나머지를 추론하거나 생략하지 않는다.
- **읽었으면 따른다.** 읽어놓고 안 따르는 것은 안 읽은 것과 같다.
- 프로젝트에 기존 패턴(폴더 구조, 파일 위치 등)이 존재하더라도, 이 문서의 규칙이 우선한다.

---

## 0.1 문서 작성 절대 규칙

**이 규칙은 plan.md, context.md, checklist.md, learned.md 등 모든 산출물 작성 시 적용된다.**

### 코드 스니펫은 반드시 포함한다
- plan.md의 "참고 코드 스니펫" 섹션, learned.md의 "사용 예시" 섹션을 **비워두지 않는다.**
- 코드 블록(```)이 있는 섹션에는 **실제 코드**를 작성한다. 플레이스홀더(`(여기에 작성)`)로 남기지 않는다.
- 코드는 **메모리에서 재현하지 않는다.** 반드시 해당 파일을 Read 도구로 다시 열어서 복사한다.

### 문서 작성 전 파일 재읽기
- 문서(특히 learned.md)를 작성하기 **직전에**, 이번 작업에서 생성/수정한 모든 파일을 다시 열어서 읽는다.
- "아까 읽었으니 기억한다"고 하지 않는다. **다시 읽는다.**
- 읽은 코드에서 필요한 부분을 문서에 직접 옮긴다.

### 토큰 절약 금지
- 문서 작성에서 토큰을 아끼지 않는다.
- 코드가 길더라도 의미 있는 전체 단위(함수, 클래스, 설정 블록)를 포함한다.
- `... (생략)` 또는 `// 나머지 코드`로 축약하지 않는다.

---

## 1. 작업 분류 및 파이프라인

요구사항을 받으면 **바로 구현에 착수하지 않는다.** 아래 순서로 작업을 분류한다.

### 1.1 작업 유형 판단 (최우선)

| 유형 | 판단 신호 | 핵심 위험 |
|------|----------|----------|
| **버그 수정** | "안 돼", "에러가", "깨졌어", "동작 안 해", "수정해줘", "고쳐줘" + 기존 기능이 기대와 다르게 동작 | **오진** — 원인을 잘못 파악하고 수정 |
| **기능 구현** | "만들어줘", "추가해줘", "구현해줘", "새로운 ~" + 이전에 없던 것을 만드는 것 | **설계 오류** — 잘못된 방향으로 구현 |
| **리팩토링** | "리팩토링", "정리해줘", "개선해줘", "구조를 바꿔줘" + 동작은 유지하면서 코드 품질을 변경 | **기존 기능 파괴** — 동작이 바뀜 |

**판단이 어려운 경우:**
- "이거 고쳐줘"가 버그 수정인지 기능 변경인지 모호하면 → 사용자에게 질문한다.
- 하나의 요청에 여러 유형이 섞여 있으면 → 유형별로 분리하여 순차 진행한다.
- 완전히 모호하면 → **버그 수정 파이프라인**(조사 우선)으로 시작한다. 더 안전하다.

### 1.2 작업 규모 판단

유형 판단 후 규모를 판단한다:

| 규모 | 기준 | 예시 |
|------|------|------|
| **소** | 1~2개 파일 수정 | 로그인 버그 수정, 환경 변수 추가 |
| **중** | 3~10개 파일 변경 | CRUD API 추가, 인증 기능 구현 |
| **대** | 10개+ 파일 또는 아키텍처 변경 | 결제 모듈 신규, 모놀리스 분리 |

판단이 어려우면 사용자에게 확인한다.

#### 위험 승격 (파일 수와 무관)

아래 영역은 파일 수가 적어도 **최소 중규모로 승격**한다 (페이즈 게이트·문서 강도 상향):

> 인증/권한 · 결제 · 데이터 삭제/마이그레이션 · public API · 빌드 설정 · 테스트 인프라 · 대량 이동/삭제 · 동시성/성능 핵심부 · DB 스키마 변경.

#### 규모 결정 우선순위 (사용자 지정 최우선 + 위험 하한) — ★ 조용한 축소 금지 ★

규모는 아래 우선순위로 확정한다.

1. **사용자가 규모를 명시하면 그 규모를 최우선으로 따른다.** (`orchestration.md §2.4` "사용자 명시 기준 최우선"과 일관) AI가 다시 판단해 덮어쓰지 않는다.
2. **단, 위험 승격 영역은 최소 규모 하한을 가진다.** 사용자 지정 규모가 그 하한보다 낮으면 **자동 하향 금지** — 결제·인증·데이터 마이그레이션 변경을 사용자가 "소규모"라 해도 조용히 소규모로 처리하지 않는다.
3. **충돌 시 게이트로 노출한다.** "이 변경은 위험 승격 영역(예: 결제)이라 최소 중규모를 권고합니다. 그래도 소규모로 진행할까요?"를 AskUserQuestion으로 확인한다. **AI가 사용자를 막는 게 아니라 충돌을 보이게 하는 것**이다.
4. **사용자가 재확인하면 그 결정대로 진행하고 기록한다.** 최종 결정권은 사용자에게 있다.
5. **규모 결정을 기록한다** — plan.md(또는 소규모 경량 기록) 머리에 `scale_decider=user|ai` / `risk_floor_applied=true|false` / `override_confirmed=true|false`를 남긴다.

> 사용자가 규모를 지정하지 않으면 위 표 + 위험 승격으로 AI가 판단하되 `scale_decider=ai`로 기록한다. 이 우선순위가 멀티워커 캐스팅 깊이(`orchestration-agent.md` 12절 opt-in 3단계)를 결정한다.

#### 규모별 문서·검증 강도 (소/중/대 모델)

| 규모 | 산출물 | 검증 |
|------|--------|------|
| **소** (1~2파일, 위 위험영역 아님) | **경량 기록 1개**(plan 핵심 + 변경/검증 5줄). context/checklist 생략 가능, learned 선택 | 최종 테스트(B5/A5/C5)만 |
| **중** (3~10파일) | plan + context + checklist (+ 페이즈 checklist 섹션) | **페이즈 게이트**(5.8) + 최종 테스트 |
| **대** (10+파일·아키텍처) | **master-plan + phases/ 분리**(5.8) + context + checklist | **페이즈 게이트** + 최종 테스트 |

- codex 의무 호출(B1.6/B3/B5 등)은 **모든 규모 유지**(사용자 결정 2026-05-14·2026-06-05). 단 페이즈마다 codex를 강제하지 않는다 — 페이즈 게이트는 로컬 테스트 중심, codex는 최종 검증 중심(5.8).
- 소규모 경량화 상세는 7.3(learned 조건부) 참조.

### 1.3 서브 파이프라인 분기

유형과 규모가 결정되면, 해당 서브 파이프라인을 따른다.

```
요구사항 수신
    │
    ├── [1] 유형 판단: 버그 / 기능 / 리팩토링
    ├── [2] 규모 판단: 소 / 중 / 대
    │
    └── 서브 파이프라인 분기
        │
        ├── 버그 수정 ──→ 4.A절
        ├── 기능 구현 ──→ 4.B절
        └── 리팩토링  ──→ 4.C절
```

### 1.4 공통 규칙 (모든 유형/규모)

- **모든 서브 파이프라인의 모든 단계(게이트)는 생략하지 않는다.** 규모에 따라 달라지는 것은 **산출물 깊이**이지, 게이트(방향성 점검·계획 승인·테스트)가 아니다.
- **산출물은 규모별 강도(1.2 표)를 따른다.** 소규모=경량 기록 1개, 중규모=3문서, 대규모=master-plan+phases. 1~2파일 수정에 4문서를 강제하지 않는다. (단 위험 승격 영역은 최소 중규모)
- **학습 기록**(learned.md)은 **조건부**다 — 7.3 참조. 소규모는 5줄 요약, 새 라이브러리·비직관 버그·테스트 전략 변경·codex 유의미 지적 시 full 승격.
- **중/대규모 구현**에서는 전체를 한 번에 구현하지 않고, **페이즈 단위로 나눠 구현 → 페이즈 게이트(5.8)로 검증 후 다음 페이즈**로 넘어간다.
- **주석/검토**(사용자가 plan.md에 주석을 달고 반복 수정)는 대규모에서 수행한다.

---

## 2. 프로젝트 진입 흐름

파이프라인 시작 전, 반드시 아래 순서를 수행한다.

### 2.1 프로젝트 맥락 문서 확인

1. Claude가 호출된 **대상 프로젝트** 경로를 확인한다.
2. `docs/project-overview.md`가 존재하는지 확인한다.

#### 존재하는 경우
- `project-overview.md`를 **실제로 읽고** 프로젝트 맥락을 파악한다.
- 내용이 현재 코드와 맞는지 간략히 검증한다 (파일 구조, 주요 의존성 등).

#### 존재하지 않는 경우 (첫 진입)
- 프로젝트를 분석하고 `docs/project-overview.md`를 작성한다.
- **기존 프로젝트**: 코드, 구조, 의존성을 상세 분석하여 현재 상태를 기록한다.
- **신규 프로젝트** (코드가 없거나 거의 없는 경우): 사용자의 요구사항을 바탕으로 목표 구조를 설계하고 기록한다. 이후 구현이 진행되면서 실제 상태로 업데이트한다.
- **분석/설계 항목:**

```
# 프로젝트 개요 (Project Overview)

## 프로젝트 정보
- 프로젝트명:
- 기술 스택:
- 아키텍처 패턴:

## 디렉토리 구조
(실제 프로젝트의 폴더/파일 구조)

## 핵심 모듈/컴포넌트
| 모듈 | 경로 | 역할 |
|------|------|------|

## 의존성
| 라이브러리 | 버전 | 용도 |
|-----------|------|------|

## 설정 파일
| 파일 | 역할 |
|------|------|

## 테스트 구조
- 테스트 프레임워크:
- 실행 명령어:
- 테스트 위치:

## DB/외부 서비스
| 서비스 | 용도 | 접속 방식 |
|--------|------|----------|

## 현재 상태
- 단계: (신규/초기/개발중/운영중)
- (신규인 경우: 목표 아키텍처와 첫 구현 범위)
- (기존인 경우: 알려진 이슈나 기술 부채)
```

- 작성 후 사용자에게 검토를 요청한다.

### 2.2 프로젝트 가이드 확인

1. `docs/guide/` 디렉토리가 존재하는지 확인한다.

#### Case A: `docs/guide/`가 존재하는 경우
- 해당 가이드 문서를 읽고, 가이드에 명시된 규칙과 구조를 우선 따른다.
- 가이드와 이 오케스트레이션의 충돌 시, 프로젝트 가이드가 우선한다.

#### Case B: `docs/guide/`가 존재하지 않는 경우
- 사용자에게 선택지를 제시한다:
  - **옵션 A**: "가이드 없이 오케스트레이션 기본 규칙만으로 진행할까요?"
  - **옵션 B**: "새 가이드를 생성하면서 진행할까요?"
- 옵션 B를 선택한 경우:
  - 대상 프로젝트에 `docs/guide/` 디렉토리를 생성한다.
  - 작업을 진행하면서 결정사항, 구조, 규칙을 가이드에 기록한다.

#### Case C: 사용자가 다른 경로를 지정한 경우
- 사용자가 별도의 가이드 폴더를 지정하면 그 경로를 사용한다.

### 2.3 프로젝트 맥락 문서 업데이트 규칙

- **모든 작업의 피드백 단계 완료 후**, `project-overview.md`에 변경사항을 반영한다.
- 새 모듈 추가 → "핵심 모듈/컴포넌트" 테이블 업데이트
- 새 의존성 추가 → "의존성" 테이블 업데이트
- 아키텍처 변경 → 구조 설명 업데이트
- 이렇게 하면 **다음 세션에서 이 문서를 읽으면 프로젝트 현재 상태를 즉시 파악**할 수 있다.

---

## 3. 패턴 참조 규칙

코드/구조 패턴의 출처는 아래 우선순위를 따른다.

1. **프로젝트 가이드** (`docs/guide/`) — 존재하면 최우선.
2. **프로젝트 기존 코드 패턴** — 가이드가 없으면 기존 코드의 컨벤션을 따른다 (일관성 우선).
3. **LLM 일반 지식** — 위 둘 다 없는 신규 프로젝트인 경우, LLM의 일반 아키텍처 지식으로 작성하되 **사용자에게 선택지(장단점 포함)를 제시하고 확인받는다.**

보안은 11절 "셀프체크 리마인더"의 단계별 보안 체크리스트를 모든 단계에서 적용한다.

---

## 4. 서브 파이프라인

### 공통: 추론 금지 — 실제로 읽어라

이 규칙은 **모든 서브 파이프라인의 모든 단계**에서 적용된다.

코드를 "아마 이럴 것이다"라고 추론하지 않는다. 반드시 실제 파일을 열어서 읽는다.

- **파일 구조**: `ls`, `find`, `glob`으로 실제 구조를 확인한다. 추측하지 않는다.
- **코드 내용**: 관련 파일을 전부 열어서 읽는다. 일부만 읽고 나머지를 추론하지 않는다.
- **의존성**: `import`/`require` 문을 실제로 확인한다. 라이브러리 버전도 `package.json`, `pyproject.toml`, `build.gradle` 등에서 직접 확인한다.
- **설정**: 설정 파일(`yaml`, `toml`, `json`, `env`)을 실제로 읽는다. 기본값을 가정하지 않는다.
- **테스트**: 기존 테스트 파일이 있으면 반드시 읽어서 테스트 패턴을 파악한다.

**읽기 원칙**:
- 관련 파일이 10개면 10개를 다 읽는다. 토큰을 아끼려고 3개만 읽고 나머지를 추론하지 않는다.
- 파일이 길면 전체를 읽는다. 앞부분만 읽고 뒷부분을 추론하지 않는다.
- 함수 시그니처만 보지 않는다. 함수 본문(구현 내용)까지 읽는다.
- "이 패턴이겠지"라고 가정하지 않는다. 실제 코드가 그 패턴을 따르는지 직접 확인한다.

> **왜?**: 추론으로 절약한 토큰보다, 추론 오류로 인한 재작업 비용이 훨씬 크다. 한 번 제대로 읽는 것이 세 번 고치는 것보다 낫다.

---

### 4.A 버그 수정 파이프라인

```
[A1. 조사]
   ↓
[A1.5 외부 큐레이션]      Claude WebSearch + codex 큐레이션 (유사 버그 사례·CVE)
   ↓
[A1.6 모델 교차 검증]     codex로 조사 결과 second opinion
   ↓
[A1.7 research.md 작성]
   ↓
[A2. 진단 보고 + codex 진단 검토] ★ 필수 게이트 ★
   ↓
[A3. 수정 계획] (plan.md + codex plan 검토)
   ↓
[A4. 수정]  ──┐ (plan에서 병렬 파생)
[A4-T. 테스트 설계] (재현/회귀, spec·버그 기준, impl diff 미열람 — 6.7)
   ↓
[A4.5 리뷰 게이트] (수정자와 분리 — 5.9)
   ↓
[A5. 테스트] (재현/회귀 테스트 + codex 테스트 검증)
   ↓
[A6. 피드백]
```

> **훅 호환**: X.5 / X.6 / X.7 는 모두 `stage-transition.sh` 상으로는 `1.research` 단계 내부.

**핵심 원칙**: 버그 수정에서 가장 위험한 것은 **오진**이다. 수정보다 조사가 더 중요하다. 그래서 A2에 일반 모델 교차 검증(A1.6)과 별도로 **진단 전용 검토**를 추가 호출한다.

#### A1. 조사 (Investigate)

**목표**: 버그의 현재 동작과 원인을 정확히 파악한다. **코드를 수정하지 않는다.**

**수행 내용**:
1. 사용자의 버그 설명을 **끝까지 듣는다.** 설명 도중에 수정을 시작하지 않는다.
2. 관련 파일을 **실제로 열어서** 현재 동작을 파악한다.
3. 에러 로그, 스택 트레이스가 있으면 읽는다.
4. 버그가 발생하는 **코드 경로**를 추적한다 (호출 체인을 따라간다).
5. 관련된 설정, 환경 변수, 외부 서비스 상태를 확인한다.

**금지 사항**:
- ❌ 조사 중에 코드를 수정하지 않는다.
- ❌ "아마 이게 원인일 것이다"라고 추측하고 바로 수정하지 않는다.
- ❌ 사용자의 설명이 끝나기 전에 파일을 편집하지 않는다.

**산출물**: research.md "1. Claude 분석" 섹션에 기록.

#### A1.5 외부 큐레이션 — ★ 필수 ★

B1.5와 동일한 절차를 따른다 (Claude WebSearch + codex 외부 큐레이션 통합 → research.md 2번 섹션).

**버그 수정 고유**: 검색 키워드에 **에러 메시지·스택 트레이스 핵심 라인·관련 라이브러리 버전·CVE/이슈 ID**를 우선 포함한다. 같은 버그를 다른 사람이 이미 겪었을 가능성을 검증.

스택 트레이스를 codex 프롬프트에 넣을 때는 **5.7 보안 게이트**의 "스택 트레이스" 패턴(내부 경로/호스트명)을 redact한 뒤 전달.

#### A1.6 모델 교차 검증 — ★ 필수 ★

B1.6과 동일한 절차 (codex "2. 모델 교차 분석 프롬프트" 사용 → research.md 3·4 섹션).

**버그 수정 고유**: Claude의 1.4 "잠정 원인 가설"을 codex에 명시해 다른 가설 경로를 적극 발굴 요청.

#### A1.7 research.md 작성 — ★ 필수 산출물 ★

B1.7과 동일.

#### A2. 진단 보고 + codex 진단 검토 (Diagnosis Gate) — ★ 필수 게이트 ★

**목표**: 조사 결과를 사용자에게 보고하고, 진단이 맞는지 확인받는다. A1.6의 일반 교차 검증에 더해, **진단 전용 검토**를 codex로 한 번 더 수행한다 (오진 방지 강화).

**⚠️ 이 단계를 건너뛰고 수정으로 넘어가지 않는다. 예외 없음.**

**수행 내용**:
1. Claude의 진단을 정리한다 (현재 동작 / 기대 동작 / 원인 분석 / 영향 범위 / 수정 방향).
2. **codex 진단 검토**: `templates/codex-prompt.md` "4. 진단 검토 프롬프트"로 진단 보고 + 관련 코드 발췌를 전달, 오진 가능성 점검을 받는다.
3. codex 응답의 핵심 점검 항목을 research.md 3·4 섹션에 추가 기록.
4. 사용자에게 통합 보고.

**보고 형식**:
```
[진단 보고 + 교차 검증]
1. 현재 동작: (실제로 어떻게 동작하고 있는지)
2. 기대 동작: (어떻게 동작해야 하는지)
3. 원인 분석: (왜 현재 동작이 발생하는지 — 코드 경로와 근거)
4. 영향 범위: (이 버그가 다른 기능에 미치는 영향)
5. 수정 방향: (어떻게 고칠 것인지 — 간략하게)
6. codex 진단 검토 결과: (오진 가능성 / 다른 원인 가설 / Claude가 놓친 재현 조건 등 — 채택/기각 결정 포함)
7. 멀티워커 캐스팅(대/고위험): 제안 페르소나 + 규모(scale_decider) — `orchestration-agent.md` 12절
```

**진행 조건**:
- 사용자가 진단에 **동의**하면 → A3(수정 계획)으로 진행한다.
- 사용자가 진단을 **수정**하면 → 추가 조사 후 진단을 업데이트하고, codex 진단 검토를 재호출한다.
- 사용자가 "그냥 고쳐줘"라고 하면 → A3(수정 계획)으로 진행하되, 진단 내용 + codex 검토는 기록으로 남긴다.
- codex 호출 실패 시 자동 스킵 + 사유 기록, 게이트는 그대로 진행.

#### A3. 수정 계획 (Fix Plan) — ★ codex plan 검토 의무 ★

**목표**: 수정 범위와 방법을 확정하고, codex로 plan 함정을 점검받는다.

**수행 내용**:
- 산출물은 규모별(1.2 표)로 생성한다 — 소규모: 경량 기록 1개 / 중규모: plan·context·checklist / 대규모: master-plan + phases (5.8).
- plan.md에 **변경할 파일 목록**과 **절대 변경하지 않을 파일/영역**을 명시한다.
- context.md의 "금지 영역" 섹션을 반드시 작성한다.
- 수정이 미치는 영향 범위를 명시한다.
- **codex plan 검토**: `templates/codex-prompt.md` "3. plan 검토 프롬프트"로 plan.md 검토 요청 → plan.md "7. codex 검토 결과" 섹션에 기록 → 채택 항목 본문 반영.
- 호출 실패 시 자동 스킵 + plan.md 7.4에 사유 기록.

**진행 조건**:
- 사용자가 계획을 승인하면 → A4(수정)으로 진행한다.

#### A4. 수정 (Fix)

공통 구현 규칙(5절)을 따른다.

**버그 수정 고유 규칙**:
- 수정은 **최소 범위**로 한다. 버그와 무관한 코드를 "개선"하지 않는다.
- 수정 전에 해당 파일을 **다시 읽는다.** 조사 시점과 달라졌을 수 있다.
- 수정 후 "이제 됐을 것이다"라고 넘어가지 않는다. 반드시 테스트한다.

#### A5. 테스트 (Test) — ★ codex 테스트 검증 의무 ★

공통 테스트 규칙(6절)을 따른다.

**버그 수정 고유 규칙**:
- **재현 테스트 우선**: 버그가 재현되지 않는 것을 먼저 확인한다.
- **회귀 테스트**: 수정이 다른 기능을 깨뜨리지 않았는지 확인한다.
- 가능하면 버그 재현 테스트를 코드로 작성한다 (향후 재발 방지).
- **codex 테스트 검증**: 테스트 통과 후 `templates/codex-prompt.md` "5. 테스트 검증 프롬프트"로 변경 diff + 테스트 + 버그 재현 테스트를 codex에 전달, **재발 방지가 충분한지 / 다른 재현 경로가 있는지**를 받는다. 결과는 checklist + learned.md에 기록. 호출 실패 시 자동 스킵 + 사유 기록.

#### A6. 피드백 (Feedback)

공통 피드백 규칙(7절)을 따른다.

---

### 4.B 기능 구현 파이프라인

```
[B1. 리서치]
   ↓
[B1.5 외부 큐레이션]      Claude WebSearch + codex 외부 큐레이션 통합
   ↓
[B1.6 모델 교차 검증]     codex로 Claude 분석에 대한 second opinion 수신
   ↓
[B1.7 research.md 작성]   Claude 분석 + codex 분석 + 외부 큐레이션 통합 산출물
   ↓
[B2. 방향성 점검] ★ 필수 게이트 ★
   ↓
[B3. 계획] (plan.md + codex plan 검토)
   ↓
[B4. 구현]  ──┐ (plan에서 병렬 파생)
[B4-T. 테스트 설계] (spec 기준, impl diff 미열람 — 6.7)
   ↓
[B4.5 리뷰 게이트] (spec compliance, 구현자와 분리 — 5.9)
   ↓
[B5. 테스트] (실행 테스트 + codex 테스트 검증)
   ↓
[B6. 피드백]
```

> **훅 호환**: X.5 / X.6 / X.7 는 모두 `stage-transition.sh` 상으로는 `1.research` 단계 내부에서 수행된다. 5단계 체계는 그대로 유지.

**핵심 원칙**: 기능 구현에서 가장 위험한 것은 **방향 오류**다. 잘못된 방향으로 빠르게 달리는 것보다 올바른 방향을 확인하고 시작하는 것이 낫다.

#### B1. 리서치 (Research)

**목표**: 요구사항을 정확히 이해하고, 필요한 정보를 수집한다.

**수행 내용**:
- 요구사항을 분해하여 핵심 과제와 부가 과제를 분리한다.
- 대상 프로젝트의 기존 코드, 구조, 의존성을 **실제로 열어서** 파악한다.
- 모르는 부분, 모호한 부분을 명확히 정리한다.

**산출물**: research.md의 "1. Claude 분석" 섹션에 기록 (1.1 읽은 파일 목록 / 1.2 코드 구조 요약 / 1.3 모호한 지점 / 1.4 잠정 방향성).

#### B1.5 외부 큐레이션 (External Curation) — ★ 필수 ★

**목표**: LLM의 다수결 편향을 외부 큐레이션으로 보정한다. 모델이 만든 1·2·3 옵션 메뉴 안에서만 결정하면 "4번 옵션"은 영영 사각지대로 남는다. 외부 큐레이션이 그 사각지대를 메운다.

**수행 내용 — 두 신호원을 모두 수행**:

1. **Claude WebSearch / WebFetch**: 작업 키워드 + **최신 1년 범위**로 검색을 **최소 1회** 수행한다. 결과 중 비주류/혁신/새 라이브러리/논문/실험적 접근을 식별한다.
2. **codex 외부 큐레이션**: `templates/codex-prompt.md` "1. 외부 큐레이션 프롬프트"를 사용해 codex에게 비주류·최신·반대 관점 자료를 요청한다.
   - 호출 전 codex의 web access 능력은 작업 시작 시 **codex 능력 사전 확인**(web access 가능 여부) 결과를 따른다.
   - 검색 가능 시: codex가 직접 자료 큐레이션 + 출처 URL 제공.
   - 검색 불가 시: codex는 Claude WebSearch 결과를 비평하거나 내부 추론 기반 "다른 관점" 제시.
3. **통합**: 두 결과를 research.md "2. 외부 큐레이션 통합" 섹션에 함께 기록 — 출처를 구분해 가치 차이를 사용자가 판단할 수 있게.

**의무 / 생략 기준**:
- **의무**: 새 라이브러리/패턴/도메인을 다루거나, 작업 키워드가 변화 빠른 영역(AI/검색/분산/UI 프레임워크 등)인 경우.
- **생략 가능**: 도메인 지식이 충분히 깊고, 단순 수정/리팩토링이며, 외부 변화의 영향이 없는 경우.
- 생략하는 경우 **research.md "2.4 외부 큐레이션 생략 사유"에 명시한다.** 사유 없는 자동 생략 금지.
- **codex 호출 실패 시**: 자동 스킵, 작업 중단 없음. checklist의 호출 기록 표에 사유 명시.

**왜 필요한가** (배경):
- LLM은 학습 데이터의 다수결을 따르므로, 비주류이지만 더 나은 옵션을 놓치기 쉽다.
- 외부 큐레이션은 사람이 수집한 정보 + 다른 LLM의 큐레이션을 모델 컨텍스트에 주입해, 모델의 다수결 답을 흔들어주는 장치다.
- 두 신호원은 보완재 — 사람 자료(WebSearch)와 다른 LLM(codex)이 서로 다른 사각지대를 가진다.

**진행 조건**:
- B1.5의 두 결과가 research.md에 흔적으로 남았거나, 생략 사유가 명시되어야 B1.6로 넘어간다.

#### B1.6 모델 교차 검증 (Model Cross-Validation) — ★ 필수 ★

**목표**: Claude의 분석(B1 + B1.5)에 대해 codex의 second opinion을 받는다. 사각지대 / 다른 해석 / 위험 / 불확실성을 발굴.

**수행 내용**:
1. `templates/codex-prompt.md` "2. 모델 교차 분석 프롬프트"를 사용.
2. 입력: Claude의 1.2~1.4 분석 요약 + 원본 컨텍스트 발췌(입력 크기 정책에 따라).
3. 호출 직전 보안 게이트(5.7) 패턴 스캔 — 매칭 0건일 때만 자동 통과.
4. 응답 수신 후 research.md "3. codex 분석 (모델 교차 검증)" 섹션에 호출 ID, 입력 해시(sha256 앞 12자), 응답 핵심 지적을 기록.
5. Claude 의견과 codex 의견을 research.md "4. Claude vs codex 교차 보고" 표에 정리.

**호출 실패 시**: 자동 스킵, 작업 중단 없음. research.md "3.2 호출 실패 시 사유"에 시각·사유·후속 조치 기록. 입력 해시가 이전 호출과 동일하면 캐시 응답 재사용 가능.

**진행 조건**: research.md 3·4 섹션이 채워졌거나, 호출 실패 사유가 명시되어야 B1.7로 넘어간다.

#### B1.7 research.md 작성 완료 — ★ 필수 산출물 ★

**목표**: B1 + B1.5 + B1.6의 결과를 통합하여 사용자에게 보고할 단일 산출물을 완성한다.

**수행 내용**:
- `templates/research.md` 템플릿을 사용해 다음 섹션을 모두 채운다:
  - 0. 작업 정보
  - 1. Claude 분석
  - 2. 외부 큐레이션 통합 (Claude WebSearch + codex 큐레이션)
  - 3. codex 분석 (모델 교차 검증)
  - 4. Claude vs codex 교차 보고
  - 5. 방향성 질문 (사용자에게 묻고 싶은 점)
  - 6. 호출 기록
- 저장 위치: `docs/plans/{날짜}/{작업명}/research.md` — 이후 plan.md / context.md / checklist.md와 같은 폴더.
- **소규모**: 별도 research.md 대신 **경량 기록의 research 섹션에 통합 가능**. (외부 큐레이션·codex 교차 검증 자체는 규모와 무관하게 수행하되, 흔적을 경량 기록 안에 남긴다.)

**진행 조건**: research 흔적(research.md 또는 소규모 경량 기록의 research 섹션)이 저장되어야 B2 게이트로 넘어간다.

#### B2. 방향성 점검 (Direction Gate) — ★ 필수 게이트 ★

**목표**: research.md(Claude 분석 + 외부 큐레이션 + codex 분석 통합)를 사용자에게 보고하고, **방향성**이 맞는지 확인받는다.

**⚠️ 이 단계를 건너뛰고 계획으로 넘어가지 않는다. 예외 없음.**

**보고 형식**:
```
[방향성 점검]
1. 요구사항 요약: (내가 이해한 것)
2. 기존 코드 현황: (현재 관련 코드의 상태)
3. 외부 큐레이션 요약: (Claude WebSearch + codex 큐레이션에서 발굴한 핵심 자료)
4. 모델 교차 검증 결과 요약: (Claude 의견 vs codex 의견 — 합의/갈림)
5. 구현 방향: (어떤 접근으로 구현할 것인지 — 간략하게)
6. 질문 사항: (방향성과 관련해 사용자 판단이 필요한 점 — research.md 5절 참조)
7. 멀티워커 캐스팅(대/고위험): 제안 페르소나 + 규모(scale_decider) — codex가 누락/과잉 점검. `orchestration-agent.md` 12절
```

**진행 조건**:
- 사용자가 방향성에 **동의**하면 → B3(계획)으로 진행한다.
- 사용자가 방향성을 **수정**하면 → 수정 사항을 반영하고 research.md를 갱신한 뒤 다시 보고한다.
- 사용자가 "진행해"라고 하면 → B3(계획)으로 진행한다.
- research.md 5절 질문에 답이 필요하면 — AskUserQuestion 도구로 옵션을 제시.

#### B3. 계획 (Plan) — ★ codex plan 검토 의무 ★

**목표**: 구현 방향과 순서를 구체적으로 수립하고, codex로 plan 함정을 점검받는다.

**수행 내용**:
- 구현 범위를 확정한다.
- 파일 단위로 변경 계획을 작성한다.
- 의존성 순서를 고려하여 구현 순서를 정한다.
- 리스크와 트레이드오프를 식별한다.
- **산출물은 규모별(1.2 표)로 생성한다**:
  - **소규모**: 경량 기록 1개(plan 핵심 + 변경/검증 + 5줄 learned). context/checklist 생략 가능.
  - **중규모**: `plan.md`(`templates/plan.md`) + `context.md`(`templates/context.md`) + `checklist.md`(`templates/checklist.md`).
  - **대규모**: `master-plan.md`(`templates/master-plan.md`) + `phases/phase-NN-*.md`(`templates/phase-plan.md`) + context + checklist. (5.8)
- plan.md에 **변경할 파일 목록**과 **절대 변경하지 않을 파일/영역**을 반드시 명시한다.
- plan.md 작성 직후 **codex로 plan 메타 검토**를 수행한다:
  - `templates/codex-prompt.md` "3. plan 검토 프롬프트" 사용.
  - 응답을 plan.md "7. codex 검토 결과" 섹션에 항목별로 채택/기각/사용자 재확인 결정과 함께 정리.
  - 채택된 지적은 plan 본문에 반영 후 사용자 보고.
  - 호출 실패 시 자동 스킵 + plan.md 7.4에 사유 기록.

**문서 저장 경로 규칙**:
- 경로: `docs/plans/{날짜}/{작업명}/` (예: `docs/plans/2026-03-24/multi-variant-terms/`)
- 날짜: 작업 시작일 (YYYY-MM-DD)
- 작업명: 케밥 케이스로 작업 내용을 간결하게 (예: `multi-variant-terms`, `boost-tuning`)
- 해당 폴더 안에 plan.md, context.md, checklist.md, research.md를 생성한다 (research.md는 B1.7에서 이미 생성됨).

**진행 조건**:
- 기술 선택이 필요한 경우 → 선택지를 장단점과 함께 제시하고 사용자가 결정한다.
- **소규모/중규모**: 3가지 문서를 생성하고 + codex plan 검토 결과를 함께 사용자에게 제시 → 승인 후 구현으로.
- **대규모**: 3가지 문서 + codex 검토 결과를 **파일로 저장** → 사용자에게 파일 경로를 안내 → 주석/검토 단계로.

**대규모 주석/검토**:
1. AI가 계획 단계에서 생성한 `plan.md` + codex 검토 결과를 사용자에게 제출한다.
2. **사용자가 plan.md를 읽고 주석/메모를 단다.**
3. AI는 주석을 반영하여 계획을 업데이트한다.
4. **사용자가 승인할 때까지 반복한다.**
5. 이 단계에서는 코드를 작성하지 않는다.

#### B4. 구현 (Implement)

공통 구현 규칙(5절)을 따른다.

#### B5. 테스트 (Test) — ★ codex 테스트 검증 의무 ★

공통 테스트 규칙(6절)을 따른다.

**기능 구현 고유 규칙**:
- **새 기능 테스트**: 구현한 기능이 정상 동작하는지 테스트한다.
- **회귀 테스트**: 기존 기능을 깨뜨리지 않았는지 확인한다.
- 가능하면 새 기능에 대한 테스트 코드를 작성한다.
- **codex 테스트 검증**: 모든 테스트 통과 후, `templates/codex-prompt.md` "5. 테스트 검증 프롬프트"로 변경 diff + 테스트 코드를 codex에 전달, 누락 엣지 케이스 / 회귀 위험 / 테스트 자체 약점을 받는다. 응답은 checklist.md "단계별 codex 호출 기록"에 기록하고, 핵심 지적은 learned.md "11. 모델 교차 검증 기록"에 정리.
- 호출 실패 시 자동 스킵 + 사유 기록.

#### B6. 피드백 (Feedback)

공통 피드백 규칙(7절)을 따른다.

---

### 4.C 리팩토링 파이프라인

```
[C1. 현상 분석]
   ↓
[C1.5 외부 큐레이션]      Claude WebSearch + codex 큐레이션 (유사 리팩토링 사례·패턴)
   ↓
[C1.6 모델 교차 검증]     codex로 현상 분석 + 위험 요소 second opinion
   ↓
[C1.7 research.md 작성]
   ↓
[C2. 목표 정의] ★ 필수 게이트 ★
   ↓
[C3. 계획] (plan.md + codex plan 검토)
   ↓
[C4. 단계적 변환]  ──┐ (plan에서 병렬 파생)
[C4-T. 테스트 설계] (회귀·동작 불변 기준, impl diff 미열람 — 6.7)
   ↓
[C4.5 리뷰 게이트] (변환자와 분리, 동작 불변 검증 — 5.9)
   ↓
[C5. 회귀 테스트] (기존 테스트 + codex 테스트 검증)
   ↓
[C6. 피드백]
```

> **훅 호환**: X.5 / X.6 / X.7 는 모두 `stage-transition.sh` 상으로는 `1.research` 단계 내부.

**핵심 원칙**: 리팩토링에서 가장 위험한 것은 **기존 기능 파괴**다. 동작은 변하지 않아야 한다.

#### C1. 현상 분석 (Current State Analysis)

**목표**: 현재 코드의 상태와 문제를 정확히 파악한다.

**수행 내용**:
1. 리팩토링 대상 코드를 **전부 읽는다.**
2. 해당 코드가 **호출되는 모든 곳**을 파악한다 (Grep/Explore 활용).
3. 기존 테스트가 있는지 확인한다. 없으면 사용자에게 보고한다.
4. 현재 코드의 **문제점**을 구체적으로 정리한다 (추상적인 "더러운 코드" 대신 구체적 이슈).

**산출물**: research.md "1. Claude 분석" 섹션에 기록 (1.2는 "현재 구조 + 호출처 매핑 + 문제점 목록").

#### C1.5 외부 큐레이션 — ★ 필수 ★

B1.5와 동일한 절차 (Claude WebSearch + codex 외부 큐레이션 통합 → research.md 2번 섹션).

**리팩토링 고유**: 검색 키워드에 **현재 패턴 + 대안 패턴 + 마이그레이션 사례**를 우선 포함. "전부 다시 짜자"의 유혹을 줄이려면, 점진적 리팩토링 사례가 특히 가치 있음.

#### C1.6 모델 교차 검증 — ★ 필수 ★

B1.6과 동일한 절차. **리팩토링 고유**: codex 프롬프트에 "동작 불변을 깨뜨릴 가능성이 있는 변환 위험"을 명시적으로 발굴 요청.

#### C1.7 research.md 작성 — ★ 필수 산출물 ★

B1.7과 동일.

#### C2. 목표 정의 (Target Definition Gate) — ★ 필수 게이트 ★

**목표**: research.md(현상 분석 + 외부 큐레이션 + codex 위험 분석 통합)를 기반으로 리팩토링의 범위와 목표를 사용자와 합의한다.

**⚠️ 이 단계를 건너뛰고 리팩토링을 시작하지 않는다. 예외 없음.**

**보고 형식**:
```
[리팩토링 목표 + 교차 검증]
1. 현재 문제: (구체적인 코드 문제 — 파일/함수 수준)
2. 목표 상태: (리팩토링 후 어떤 모습이 되는지)
3. 변경 범위: (어떤 파일/모듈을 변경하는지)
4. 동작 불변 약속: (외부에서 관찰 가능한 동작이 변하지 않음을 명시)
5. 테스트 전략: (기존 테스트 존재 여부 + 리팩토링 검증 방법)
6. 외부 큐레이션 + 모델 교차 검증 요약: (유사 사례 + codex가 지적한 위험 요소)
7. 멀티워커 캐스팅(대/고위험): 제안 페르소나 + 규모(scale_decider) — `orchestration-agent.md` 12절
```

**진행 조건**:
- 사용자가 목표에 **동의**하면 → C3(계획)으로 진행한다.
- 사용자가 범위를 **조정**하면 → 반영 후 research.md 갱신 → 다시 보고한다.

#### C3. 계획 (Plan) — ★ codex plan 검토 의무 ★

**목표**: 리팩토링 순서와 각 단계의 안전장치를 수립하고, codex로 plan 함정을 점검받는다.

**수행 내용**:
- 산출물은 규모별(1.2 표)로 생성한다 — 소규모: 경량 기록 1개 / 중규모: plan·context·checklist / 대규모: master-plan + phases (5.8).
- plan.md에 **단계별 변환 순서**를 명시한다. 한 번에 모든 것을 바꾸지 않는다.
- 각 단계 후 **검증 방법**(테스트 실행 또는 동작 확인)을 명시한다.
- context.md의 "금지 영역" 섹션에 **동작이 변하면 안 되는 인터페이스**를 명시한다.
- **codex plan 검토**: `templates/codex-prompt.md` "3. plan 검토 프롬프트"로 plan.md 검토 — 특히 "단계 사이에 코드가 깨질 가능성 / 롤백이 어려운 지점"을 명시적으로 발굴 요청. 응답은 plan.md "7. codex 검토 결과" 섹션에 기록.
- 호출 실패 시 자동 스킵 + plan.md 7.4에 사유 기록.

**진행 조건**: 사용자 승인 후 진행.

#### C4. 단계적 변환 (Stepwise Transform)

공통 구현 규칙(5절)을 따른다.

**리팩토링 고유 규칙**:
- **한 단계씩** 변환한다. 단계 사이에 테스트를 실행한다.
- 각 단계는 **독립적으로 동작 가능한 상태**를 유지한다. 중간에 멈춰도 코드가 깨지지 않아야 한다.
- 동작을 변경하는 코드 수정이 감지되면 **즉시 멈추고 사용자에게 보고**한다.
- 리팩토링 범위를 넘어서는 "추가 개선"을 하지 않는다.

#### C5. 회귀 테스트 (Regression Test) — ★ codex 테스트 검증 의무 ★

공통 테스트 규칙(6절)을 따른다.

**리팩토링 고유 규칙**:
- **기존 테스트 100% 통과**가 최우선 목표다.
- 기존 테스트가 없었다면, 리팩토링 전 동작을 보장하는 테스트를 추가한다.
- 테스트 실패 시, 리팩토링 전 코드로 **롤백할지 수정할지** 사용자에게 확인한다.
- **codex 테스트 검증**: `templates/codex-prompt.md` "5. 테스트 검증 프롬프트"로 변경 diff + 기존 테스트 + 추가 테스트를 codex에 전달, **회귀 위험 / 동작 불변 약속이 실제로 지켜졌는지 / 테스트 커버리지 빈틈**을 받는다. 결과는 checklist + learned.md에 기록. 호출 실패 시 자동 스킵 + 사유 기록.

#### C6. 피드백 (Feedback)

공통 피드백 규칙(7절)을 따른다.

---

## 5. 공통 구현 규칙

모든 서브 파이프라인의 구현/수정/변환 단계에서 적용된다.

### 5.1 구현 중 읽기 규칙

- 수정할 파일을 열 때, 수정 지점 주변만 보지 않는다. **파일 전체**를 읽고 맥락을 파악한 뒤 수정한다.
- 다른 파일에서 호출되는 함수를 수정할 때, **호출하는 쪽도 실제로 읽어서** 영향 범위를 확인한다.
- "이 파일은 아까 읽었으니 기억나겠지"라고 하지 않는다. 필요하면 **다시 읽는다**.

### 5.2 범위 통제 규칙

**⚠️ 이 규칙은 가장 중요한 규칙 중 하나다.**

- **파일 수정 전 대조**: Edit/Write 도구를 사용하기 전에, 해당 파일이 plan.md의 "변경 대상 파일" 목록에 있는지 확인한다.
- **목록에 없는 파일 수정 금지**: plan.md에 없는 파일을 수정해야 하는 상황이 발생하면, **즉시 멈추고 사용자에게 보고**한다. 사용자가 승인한 후에만 수정한다.
- **계획에 없는 "개선" 금지**: 계획에 없는 코드 개선, 리팩토링, 포매팅 변경을 임의로 하지 않는다.
- **금지 영역 절대 준수**: context.md의 "금지 영역"에 명시된 파일/기능은 어떤 이유로든 수정하지 않는다.
- **단일 변경 원칙**: 한 번의 구현 단계에서는 **하나의 의도만** 변경한다. 단, "하나의 의도"는 **중간 검증(빌드/테스트)이 가능한 단위**를 뜻한다 — 컴파일조차 안 되는 원자 변경까지 쪼개지 않는다.
  - 금지 예: 예외 구조 변경 + 서비스 상속 변경 + 폴더 이동을 동시에 / DB source 변경 + DTO 변경 + controller 응답 변경을 동시에 / 포팅 중 원본 주석 정리 + entity 삭제 + data flow 변경을 동시에.
  - 허용 예: 예외 클래스 이름만 통일 → 빌드 확인 / repository 메서드 하나만 JPA로 전환 → 테스트 확인 / 한 국가 migration loader만 수정 → row count 확인.
  - 다음 변경으로 넘어가기 전 **최소 하나의 검증**을 수행한다. (중/대규모는 5.8 페이즈 게이트로 강제)

### 5.3 파일 보호 규칙

- **기존 파일을 Write 도구로 덮어쓰기 전에**, 반드시 Read로 현재 내용을 확인한다.
- 기존 문서(docs/ 내의 .md 파일)를 덮어쓸 때는 사용자에게 확인한다.
- Edit 도구(부분 수정)를 Write 도구(전체 덮어쓰기)보다 우선 사용한다.

### 5.4 구현 수행 내용

- 승인된 계획의 구현 순서에 따라 코드를 작성한다.
- 사용자가 주석으로 지시한 방향을 따른다.
- 구현 중 계획과 다른 상황이 발생하면 멈추고 사용자에게 보고한다.

### 5.5 대규모 구현 규칙

- 전체를 한 번에 구현하지 않는다. 모듈 또는 파일 단위로 나눠서:
  1. 해당 단위 구현
  2. 컴파일/구문 오류 없는지 확인
  3. 관련 테스트 실행 가능하면 중간 실행
  4. 다음 단위로 이동
- 이렇게 하면 최종 테스트 단계에서 실패 원인 추적이 쉬워진다.

### 5.6 코드 주석 규칙

- 구현 중에는 코드에 주석을 달지 않는다. **코드 작성에 집중한다.**
- 코드 주석은 **모든 구현이 완료되고 테스트가 통과한 후** 피드백 단계에서 추가한다.
- 주석은 "무엇을 하는가"가 아니라 **"왜 이렇게 했는가"**를 설명한다.

### 5.7 codex 호출 시 보안 게이트 — ★ 패턴 기반 ★

codex 호출 시 외부 LLM에 텍스트를 전송한다. 단순 `.env` 파일명 차단이 아니라 **내용 패턴/데이터 유형** 기반 스캔을 호출 직전에 수행한다.

**스캔 대상 (호출 직전 자동 스캔, 매칭 발견 시 사용자에게 보고)**:

| 분류 | 패턴 / 키워드 예시 | 차단 동작 |
|------|---------------------|---------|
| 시크릿 키 | `[A-Za-z0-9_-]{32,}=`, `BEGIN (RSA\|EC\|OPENSSH) PRIVATE KEY`, `xox[a-z]-`, `sk-[A-Za-z0-9]{20,}`, `ghp_[A-Za-z0-9]{20,}`, `AKIA[0-9A-Z]{16}` | 자동 마스킹 또는 호출 중단 |
| 환경 변수 본체 | `\.env(\.\w+)?$` 파일 내용, `process\.env\.\w+\s*=` 의 값 부분 | 키 이름만 남기고 값 `[REDACTED]` |
| 비밀번호/토큰 어휘 | `password\s*[:=]`, `token\s*[:=]`, `secret\s*[:=]`, `Authorization:\s*Bearer\s` | 값 부분 `[REDACTED]` |
| PII | 이메일·전화·주민/사업자번호 정규식 | `[REDACTED-PII]` |
| 스택 트레이스 | 내부 절대 경로 + 호스트명 | 경로 anonymize |
| 비공개 식별자 | 사내 이슈 ID(`[A-Z]+-\d+`), 비공개 URL(`*.internal`, `*.corp` 등) | redact 또는 사용자 확인 |

**자동 통과 조건**: 매칭 0건일 때만. 매칭 발견 시 → 사용자에게 보고 → redact 또는 호출 중단 결정 수신 후 진행.

**저장 위치**: 보안 게이트 동작 결과는 checklist.md "단계별 codex 호출 기록" 표의 "응답 요약 / 스킵 사유" 컬럼에 함께 기록 (예: "패턴 스캔 매칭 2건 redact 후 호출").

상세 패턴 표는 `templates/codex-prompt.md` 0.2절도 참조 (동일 정의 동기화).

---

## 5.8 페이즈 게이트 (중/대규모 + 위험 승격) — ★ over-scoping 방지 핵심 ★

usage report 1위 마찰은 **over-scoping**(여러 변경을 한 번에 묶어 빌드/흐름을 깨뜨림)이다. 5.2 단일 변경 원칙의 **강제 수단**으로, 중/대규모와 위험 승격 작업은 페이즈로 나누고 **각 페이즈를 검증 게이트로 통과**시킨다.

### 적용 대상
- **소규모**(1~2파일, 위험영역 아님): **면제** — 최종 테스트(B5/A5/C5)만.
- **중규모**(위험 승격으로 중규모가 된 작업 포함): plan 1개 안에 **페이즈 checklist 섹션 + 페이즈 게이트**.
- **대규모**(또는 위험 작업이 대규모인 경우): **master-plan + phases/ 분리 문서**(아래 구조).
> 위험 승격은 "**최소 중규모**"(1.2)다. 위험하다고 무조건 master+phases가 아니라, 파일 수·아키텍처 영향이 대규모일 때만 분리 문서를 쓴다.

### 페이즈 게이트 규칙 (버그/기능/리팩토링 공통)
1. 각 구현 페이즈 종료 시 **검증(빌드 + 로컬 테스트)을 통과해야 다음 페이즈에 진입**한다.
2. **역할 분리**: 페이즈 게이트 = **로컬 빌드/테스트 중심**(빠름, 매 페이즈). 최종 B5/A5/C5 = **전체 회귀 + codex 테스트 검증**(무거움, 1회). **페이즈마다 codex를 강제하지 않는다**(비용).
3. 각 페이즈는 **독립적으로 검증 가능**해야 한다. 문서만 쪼개고 검증 불가하면 무의미.
4. **검증 명령을 구체적으로 명시**한다(예: `./mvnw -pl board test`). 가벼운 명령으로 통과 처리하지 않는다.
5. 중/대규모 페이즈 종료 시, 필요하면 **사용자 확인 게이트**도 둔다 — green build만으로는 요구사항 오해를 못 잡는다.

### 실패 처리 (자동 롤백 금지)
1. 게이트 실패 시 **다음 페이즈 진입 금지**.
2. 같은 페이즈 안에서 제한적으로 수정.
3. 같은 게이트가 **2회 실패하면 3번째 시도 전 사용자에게 보고**한다(13.2 반복 실패 정지와 동일 임계). 범위 변경이 필요한 경우에도 즉시 보고.
4. 롤백은 **사용자 승인 후** 수행.
5. 예외: **AI가 방금 만든 미커밋 신규 파일**처럼 영향 범위가 명확한 것만 자동 정리 가능.

### 페이즈 분리 문서 구조 (대규모)
```
docs/plans/YYYY-MM-DD/작업명/
  master-plan.md      # 전체 범위 + 페이즈 경계 + 페이즈 간 의존성 + acceptance criteria
  context.md
  checklist.md        # 페이즈별 게이트 통과 추적
  phases/
    phase-01-foundation.md
    phase-02-api.md
    phase-03-ui.md
```
- master-plan.md 템플릿: `templates/master-plan.md`. 각 phase 문서 템플릿: `templates/phase-plan.md`.
- 각 phase 문서 필수 필드: **목표 / 변경 파일 / 변경 금지 / 완료 조건 / 검증 명령 / 실패 시 되돌릴 범위**.
- 페이즈 간 의존성이 강하면 **master-plan에 dependency + acceptance criteria**를 명시한다 (중간 green이 가짜가 되지 않도록).

---

## 5.9 코드리뷰 게이트 (X4.5) — spec compliance review ★ 구현자와 분리 ★

구현(X4) 후, 테스트(X5) 전에 **구현자와 다른 리뷰어**가 변경을 검토하는 게이트. [생성자≠검증자] 원칙. 설계 근거: `docs/08-멀티워커-오케스트레이션-설계안.md` D3 + 코드리뷰 가이드레일.

> **훅 호환**: X4.5는 `stage-transition.sh` 상으로 `4.test` 진입 직전(구현~테스트 사이)에 수행. 5단계 체계는 유지.

### 적용 대상 (규모별)
- **소규모**: 면제 — 셀프체크(11절)로 대체.
- **중규모**: 리뷰어 1명(메인이 직접 또는 별도 워커) 단일 패스.
- **대/고위험**: 별도 리뷰 워커 — 구현 워커와 **반드시 분리**.

### 리뷰 관점 (단순 버그탐색이 아니라 spec compliance)
1. plan ↔ 구현 불일치 (plan "변경 대상 파일" 범위 초과 포함 — 5.2 연동)
2. 테스트가 spec을 검증하는가, 구현을 따라갔는가
3. 실패/엣지 케이스 누락
4. 기존 동작 회귀 가능성
5. context.md "금지 영역" 침범 여부

### 가이드레일 (오탐 억제 — 양보다 질)
- **신뢰도 0~100, ≥80만 보고.** 앵커: 80=실제 hit 가능성 크고 기능에 직접 영향 / 100=빈번 발생 증거 직접 입증.
- **제보하지 않는다(false positive)**: 선재 이슈 / 린터·타입체커·컴파일러가 잡을 것 / 의도된 변경 / silence된 위반 / **사용자가 수정하지 않은 줄**.
- **생성≠채점**: 발견자와 신뢰도 채점자를 가능하면 분리. 라벨("보안리뷰어")로 약한 지적을 과대평가하지 않는다.
- **보고 형식**: `file:line` 인용, 등급 그룹화(Critical/Important/Suggestion). 위치는 코드가 가리키게(LLM 줄번호 환각 금지).

### codex와의 관계
이 리뷰 게이트는 **같은류(Claude) 검토**다. codex 테스트 검증(6.5)은 **다른 모델** 독립 신호로 별개 유지 — 둘은 보완재. 중복 발견은 메인이 dedup.

### 진행 조건
- 리뷰 결과(발견 + 신뢰도)를 checklist에 기록. Critical 이슈가 있으면 수정 후 재검토. 없으면 X5로.
- **최종 판정은 메인** — 리뷰 워커 결과를 투표 집계하지 않고 근거 품질로 재평가.

---

## 6. 공통 테스트 규칙

모든 서브 파이프라인의 테스트 단계에서 적용된다.

**⚠️ 이 단계는 모든 규모에서 생략하지 않는다.**

### 6.1 수행 내용

- 기존 테스트가 있으면 먼저 전체 테스트를 실행한다 (회귀 테스트).
- 새로 작성한 기능에 대한 테스트를 작성하고 실행한다.
- 테스트 실행 명령어는 프로젝트의 기존 방식을 따른다 (`npm test`, `pytest`, `go test`, `./gradlew test` 등).

### 6.2 테스트 반복 루프

```
구현 → 테스트 실행 → 실패?
  → YES: 실패 원인 분석 → 코드 수정 → 테스트 재실행 (반복)
  → NO: 다음 단계로
```

### 6.3 실패 시 규칙

- 테스트 실패 원인을 **실제 에러 메시지를 읽고** 파악한다 (추론 금지).
- 수정 후 반드시 **다시 테스트를 실행**한다. "이제 됐을 것이다"라고 넘어가지 않는다.
- 3회 이상 실패가 반복되면 사용자에게 보고하고 방향을 확인한다.

### 6.4 산출물

- 테스트 실행 결과 (통과/실패)
- 새로 작성한 테스트 코드
- 실패 시 수정 이력

### 6.5 codex 테스트 검증 (모든 서브 파이프라인 의무)

테스트 통과 후, `templates/codex-prompt.md` "5. 테스트 검증 프롬프트"를 사용해 codex에 변경 diff + 테스트 코드를 전달, **누락 엣지 케이스 / 회귀 위험 / 테스트 자체 약점**을 받는다.

**입력 구성** (입력 크기 정책 준수):
1. 변경 사항(git diff 또는 변경 전/후 발췌) — 항상 포함.
2. 작성한 테스트 코드 전체 또는 핵심 발췌.
3. 변경된 함수를 호출하는 다른 비변경 코드 — 의존성 점검 필요 시.

**처리 절차**:
- 호출 직전 5.7 보안 게이트 패턴 스캔.
- 응답 수신 후 checklist.md "단계별 codex 호출 기록"에 호출 ID, 입력 해시, 응답 요약 기록.
- codex가 지적한 누락 케이스 중 **반영 가치 있는 항목**은 테스트 코드를 추가하고 재실행.
- 사용자에게 핵심 지적 + 반영 결과를 보고.
- 호출 실패 시 자동 스킵 + 사유 기록, 작업 중단 없음.

**중복 방지**: 입력 해시가 직전 호출과 동일하면(예: 같은 diff·테스트로 재시도) 캐시된 응답 재사용.

---

## 6.6 데이터/마이그레이션 작업 특칙 — ★ 검증을 "실행 성공"으로 보지 않는다 ★

DB, ES, CSV, dump, parser, loader, migration, indexing 작업에서는 완료 조건을 **"스크립트가 에러 없이 돌았다"로 보지 않는다.** usage report의 고성과 작업(1,288,471행 무손실 마이그레이션 등)은 전부 record-level 검증에서 나왔다.

**필수 검증**:
1. **source count vs target count** 비교.
2. **실패 / 스킵 / 중복 건수**를 분리 보고.
3. **100건 이상 sample을 field-by-field** 검증.
4. parent-child 관계가 있으면 **orphan / mislink count** 확인.
5. **schema width, padding, collation, naming strategy** 차이 확인.
6. 검증 SQL/쿼리/스크립트를 **learned.md 또는 report에 기록**(재현 가능하게).

**검증 보고 형식**:

| 항목 | source | target | 차이 | 판정 |
|------|--------|--------|------|------|
| row count | | | | |
| child rows | | | | |
| mismatches | | | | |

> 이 특칙은 데이터 작업이면 규모와 무관하게 적용한다(위험 승격 영역). 페이즈 게이트(5.8)의 "검증 명령"으로 위 count 비교를 넣는다.

---

## 6.7 테스트 설계 분리 (X4-T) — ★ 구현자와 분리, impl diff 미열람 ★

테스트는 **구현과 별도로 설계**한다. 같은 워커가 코드와 테스트를 함께 짜면 자기 코드를 통과시키는 테스트(확증편향)를 만든다. 설계 근거: `docs/08-멀티워커-오케스트레이션-설계안.md` D2.

### 핵심 규율
1. **입력은 plan/spec/context.** 테스트 설계 단계(또는 설계자)는 **구현 diff를 먼저 보지 않는다.** 보면 분리가 형식적(구현 종속 테스트)이 된다.
2. **행위 기준.** 리팩토링엔 안 깨지고 행위 변경엔 깨지는 테스트(6절 원칙).
3. **구현과 병렬 가능** — 단 plan이 명확할 때만. plan 모호 시 impl/test 해석이 발산하므로 B2/B3(또는 A2·A3 / C2·C3) 게이트에서 plan 명확성을 먼저 확보.
4. 구현 확인(실제 코드 대조)은 **테스트 실행 단계(X5)에서** 수행.

### 적용 대상 (규모별)
- **소규모**: 면제 — 구현자가 테스트도 작성하되 6절·6.5 codex 검증으로 보완.
- **중규모**: 테스트 설계를 구현과 분리된 패스로(메인이 spec 기준 먼저 설계 후 구현 검증, 또는 별도 워커).
- **대/고위험**: 별도 테스트 설계 워커 — 구현 워커와 분리, **impl diff 미열람 계약**.

### 흐름
```
[X4 구현] ──┐ (plan에서 병렬 파생)
[X4-T 테스트 설계 (spec 기준, impl 미열람)] ──┘
   ↓
[X4.5 리뷰 게이트(5.9)] → [X5 테스트 실행 + codex 검증(6.5)]
```

---

## 7. 공통 피드백 규칙

모든 서브 파이프라인의 피드백 단계에서 적용된다.

### 7.1 수행 내용

- 구현된 코드가 요구사항을 충족하는지 확인한다.
- **완성된 코드에 주석을 추가한다** — "왜 이렇게 했는가"를 중심으로.
- 셀프체크 리마인더(11절)를 수행한다.
- **학습 기록 문서(`learned.md`)를 작성한다** (7.3절 참조).
- **`docs/project-overview.md`를 업데이트한다** — 이번 작업으로 변경된 모듈, 의존성, 구조를 반영한다.
- 변경 사항을 요약하여 사용자에게 보고한다.
- 추가 개선 사항이 있으면 제안한다.

### 7.2 산출물

- 최종 변경 요약
- 셀프체크 결과
- **학습 기록 문서 (`learned.md`)**
- **업데이트된 `project-overview.md`**
- 개선 제안 (있을 경우)

### 7.3 학습 기록 작성 (Learned) — 규모별 조건부

**목표**: 구현에서 사용된 기술 요소를 학습 자료로 남긴다. 단 **규모에 따라 강도를 조절**한다 (소규모 1~2파일에 10항목 상세는 과함).

| 규모 | learned 강도 |
|------|-------------|
| **소** | **5줄 요약**: 변경 / 검증 / 새로 안 것 / codex 지적 / 후속. (아래 10항목 생략) |
| **중·대** | 아래 **10항목 full** |
| **승격 조건** | 소규모라도 ① 새 라이브러리·패턴 ② 비직관적 버그 ③ 테스트 전략 변경 ④ codex의 유의미한 지적이 있으면 **full로 승격** |

**필수 기록 항목 (중·대규모 / 승격 시)** (템플릿: `templates/learned.md`):

1. **사용된 라이브러리** — 이름, 버전, 용도, 선택 이유
2. **핵심 함수/메서드** — 시그니처, 역할, 사용 예시 + **코드 스니펫 내 함수별 상세 설명**
3. **어노테이션/데코레이터** — 소속, 역할, 동작 원리
4. **수정 전/후 코드 비교** — 기존 파일 수정 시 before/after 코드 + 변경 이유 + 변경된 함수 설명
5. **동작 구조** — 실행 흐름 (텍스트 다이어그램), 컴포넌트별 역할, 데이터 흐름
6. **디자인 패턴** — 패턴명, 적용 위치, 이유, 구조 설명
7. **설정/컨벤션** — 프로젝트에서 따른 규칙들
8. **테스트 관련 전부**:
   - 테스트 프레임워크 및 라이브러리
   - 테스트 유틸리티/헬퍼 함수
   - Mock/Stub/Spy 도구와 사용 방식
   - 테스트 어노테이션/데코레이터
   - Assertion 메서드 (전부 나열)
   - 픽스처/팩토리 패턴
9. **새로 알게 된 것** — 이번 작업에서 배운 점
10. **더 공부할 것** — 깊이 이해하지 못한 주제

**learned.md 작성 전 필수 준비**:
1. checklist.md의 "수정 기록"에서 이번 작업에서 생성/수정한 파일 목록을 확인한다.
2. 해당 파일을 **하나씩 다시 열어서 읽는다.** 메모리에 의존하지 않는다.
3. 읽으면서 사용한 라이브러리, 함수, 어노테이션, 패턴을 정리한다.
4. 정리한 내용을 기반으로 learned.md를 작성하되, **코드 예시는 파일에서 직접 복사**한다.

**작성 원칙**:
- 토큰을 아끼지 않는다. 사용한 것은 빠짐없이 기록한다.
- "이게 뭐지?" 하고 나중에 찾아볼 필요가 없도록 충분히 설명한다.
- 코드 예시를 반드시 포함한다. **`... (생략)` 금지.**
- 같은 라이브러리를 반복 사용하더라도 매번 기록한다 (반복 노출이 학습).
- 기대 수준은 `templates/learned-example.md`를 참고한다.

**저장 위치**:
- 해당 작업의 plan 폴더와 같은 위치에 저장한다.
- 경로 예시: `docs/plans/2026-03-23/사용자인증구현/learned.md`

---

## 8. 로그/산출물 저장 규칙

### 저장 위치
- 모든 로그, 계획서, 컨텍스트, 체크리스트는 **대상 프로젝트의** `docs/` 폴더에 저장한다.
- `claude_study/`에 저장하지 않는다. 대상 프로젝트에 저장한다.

### 저장 구조 예시
```
대상프로젝트/
  docs/
    project-overview.md              # 프로젝트 전체 분석 (2.1절에서 생성, 매 작업 후 업데이트)
    guide/                           # 프로젝트 가이드 (Case B에서 생성)
    plans/
      2026-03-23/                    # 날짜별 폴더
        로그인버그수정/               # 작업별 폴더
          plan.md
          context.md
          checklist.md
          learned.md
        프로필API추가/               # 같은 날 다른 작업
          plan.md
          context.md
          checklist.md
          learned.md
      2026-03-24/
        결제모듈구현/
          ...
```

### 폴더/파일 규칙
- 날짜 폴더: `docs/plans/YYYY-MM-DD/`
- 작업 폴더: `docs/plans/YYYY-MM-DD/작업명/`
- 작업 폴더 안에: 규모별 산출물(1.2 표) — **중·대규모**는 `plan.md`, `context.md`, `checklist.md`, `learned.md`(대규모는 `master-plan.md` + `phases/`). **소규모**는 경량 기록 1개(plan 핵심 + 5줄 learned)를 같은 폴더에 단일 파일로.
- **작업명은 한글 또는 영문 간략 요약** (예: `로그인버그수정`, `프로필CRUD추가`)

### 저장 시점
- **첫 진입 시**: `docs/project-overview.md` 생성 (2.1절)
- **계획 승인 후**: 날짜/작업명 폴더 생성 → (중·대규모) plan.md, context.md, checklist.md 저장 / (소규모) 경량 기록 1개
- **테스트 통과 후**: checklist.md 업데이트 (테스트 결과 기록)
- **피드백 완료 후**: learned.md 저장 + **`project-overview.md` 업데이트**

### 주의
- 프로젝트에 기존 문서 폴더(`docs/feature/` 등)가 존재하더라도, 오케스트레이션 산출물은 **반드시 위 구조(`docs/plans/YYYY-MM-DD/작업명/`)를 따른다.**
- 3절의 "기존 코드 패턴 우선" 원칙은 **코드에만 적용**되며, 산출물 저장 구조에는 적용되지 않는다.

---

## 9. 방향 조정 패턴

작업 중 방향을 조정할 때 따르는 원칙들이다.

### 좋은 건 취하고 나머진 버리기
- 기존 코드에서 잘 작동하는 부분은 유지한다.
- 불필요하거나 비효율적인 부분만 교체한다.
- "전부 다시 짜자"는 피한다.

### 과감하게 쳐내기
- 불필요한 추상화, 사용하지 않는 코드, 과도한 설정은 제거한다.
- 단, 제거 전에 사용자에게 확인한다.

### 건드리면 안 되는 선 긋기
- 사용자가 "이건 바꾸지 마"라고 한 부분은 절대 건드리지 않는다.
- 작업 시작 전에 변경 금지 영역을 명확히 확인한다.

### 기술 선택은 사용자가 하기
- 프레임워크, 라이브러리, 아키텍처 패턴 등 기술 선택은 AI가 결정하지 않는다.
- 선택지를 장단점과 함께 제시하고, 사용자가 결정한다.

---

## 10. 작업 중 수정 요청 대응

파이프라인 진행 중 사용자가 방향 변경이나 수정을 요청하면 아래 절차를 따른다.

### 절차

1. **즉시 현재 작업을 중지한다.**
2. **진행상황을 기록한다:**
   - 어디까지 완료했는가 (checklist.md가 있으면 업데이트)
   - 현재 상태에서 동작하는 코드인가, 미완성인가
   - 변경된 파일 목록
3. **사용자의 수정 요청을 정확히 파악한다:**
   - 무엇이 변경되었는가
   - 기존 작업을 이어가는 것인가, 방향 자체가 바뀐 것인가
4. **새 plan을 작성한다:**
   - 기존 plan에서 변경된 부분만 반영한 새 plan을 작성한다
   - 기존 plan은 삭제하지 않고 보존한다 (히스토리)
   - 새 plan에 "이전 plan에서 변경된 사항"을 명시한다
5. **새 plan에 대해 사용자 승인을 받는다.**
6. **승인 후 새 plan에 따라 구현을 재개한다.**

### 규모별 적용

| 규모 | 수정 요청 시 |
|------|------------|
| **소** | 채팅으로 변경 내용 정리 → 새 계획 제시 → 승인 → 재개 |
| **중** | checklist.md에 진행상황 기록 → 새 plan.md 작성 → 승인 → 재개 |
| **대** | checklist.md 업데이트 → 새 plan.md 작성 → 사용자 주석/검토 → 승인 → 재개 |

### 핵심 원칙
- 수정 요청이 들어오면 **절대 기존 작업을 계속 밀고 나가지 않는다.**
- 이미 구현한 코드 중 수정 요청과 충돌하는 부분이 있으면 사용자에게 알린다.
- 기존 plan은 덮어쓰지 않는다. 새 파일로 생성한다.

---

## 11. 셀프체크 리마인더

구현 완료 후, 피드백 단계에서 아래 항목을 스스로 점검한다.

> **규모 단서**: 소규모(plan/context 생략)는 "plan.md 변경 대상 외 / context.md 금지영역" 항목을 **N/A** 처리하고, 경량 기록의 변경 범위로 대체 점검한다. codex 의무 항목은 모든 규모 유지.

### 체크리스트
- [ ] 요구사항을 빠짐없이 구현했는가?
- [ ] 계획에 없는 변경을 하지 않았는가?
- [ ] plan.md의 "변경 대상 파일" 외의 파일을 수정하지 않았는가?
- [ ] context.md의 "금지 영역"을 건드리지 않았는가?
- [ ] 기존 기능을 깨뜨리지 않았는가?
- [ ] 에러 처리가 적절한가?
- [ ] 코드 스타일이 프로젝트 기존 스타일과 일관되는가?
- [ ] 불필요한 코드나 주석이 남아있지 않은가?
- [ ] 보안 관련 민감 정보가 노출되지 않았는가?
- [ ] 모든 의무 단계(B1.5/B1.6/B3/B5 또는 A1.5/A1.6/A2/A3/A5 또는 C1.5/C1.6/C3/C5)에서 codex 호출이 수행되었거나, 실패/스킵 사유가 기록되었는가?
- [ ] research.md / plan.md / learned.md에 codex 응답의 핵심 지적이 누적 기록되었는가?
- [ ] checklist.md "단계별 codex 호출 기록" 표가 빠짐없이 채워졌는가?
- [ ] codex 프롬프트 전송 직전 5.7 보안 게이트 패턴 스캔이 수행되었는가?

### 단계별 보안 체크리스트 (필수)

> 각 파이프라인 단계마다 해당 항목을 점검한다. 특히 시크릿 하드코딩, SQL injection, XSS, 경쟁 조건, 에러 정보 노출을 확인한다.

**리서치/조사 단계**
- [ ] 기존 인증/인가 방식 파악
- [ ] 시크릿 관리 방식 파악
- [ ] 알려진 보안 취약점 확인 (CVE)
- [ ] 의존성 보안 상태 확인

**계획 단계**
- [ ] 새로운 보안 위험 요소 식별
- [ ] 인증/인가 변경이 필요한지 확인
- [ ] 입력 검증 전략 수립
- [ ] 민감 데이터 흐름 파악

**구현 단계**
- [ ] SQL injection 방지 (파라미터 바인딩)
- [ ] XSS 방지 (출력 이스케이프)
- [ ] CSRF 방지 (토큰 또는 SameSite)
- [ ] 경쟁 조건 방지 (DB 제약 + 잠금)
- [ ] 시크릿 하드코딩 없음
- [ ] 에러 메시지에 내부 정보 없음
- [ ] 로그에 민감 정보 없음

**피드백 단계**
- [ ] 의존성 취약점 스캔 실행 (`npm audit`, `pip-audit`, `govulncheck`, `trivy` 등)
- [ ] 보안 헤더 확인 (CORS, CSP, HSTS)
- [ ] Rate limiting 적용 확인
- [ ] 인증이 필요한 엔드포인트 전수 확인
- [ ] 최소 권한 원칙 준수 확인

### 점검 방법
- 변경된 파일을 하나씩 다시 읽으며 확인한다.
- diff를 검토하여 의도하지 않은 변경이 없는지 확인한다.

---

## 12. Git 작업 규칙

### 12.1 커밋 전 확인

- 커밋할 파일이 plan.md의 "변경 대상 파일"과 일치하는지 확인한다.
- 의도하지 않은 파일이 포함되어 있으면 제외한다.
- `git diff`로 변경 내용을 확인한 후 커밋한다.

### 12.2 Push 전 필수 확인 — ★ 사용자 확인 필수 ★

**⚠️ push 전에 반드시 사용자에게 아래를 확인한다:**

```
[Push 확인]
- 리모트: (origin 등)
- 브랜치: (현재 브랜치명)
- 커밋 수: (push할 커밋 수)
진행할까요?
```

- 사용자가 승인하면 push한다.
- 리모트가 여러 개인 경우, 어느 리모트에 push할지 반드시 확인한다.
- **절대 리모트/브랜치를 추측하여 push하지 않는다.**

### 12.3 위험한 Git 작업

아래 작업은 **사용자가 명시적으로 요청한 경우에만** 수행한다:
- `git push --force`
- `git reset --hard`
- `git branch -D`
- `git checkout .` (변경 사항 삭제)

---

## 13. 긴급 정지 프로토콜

### 13.1 사용자 부정 시그널 감지

사용자가 아래와 같은 시그널을 보내면 **즉시 현재 작업을 중단**한다:

| 시그널 | 예시 |
|--------|------|
| **명시적 중단** | "멈춰", "잠깐", "그만", "중단", "스톱" |
| **방향 부정** | "아니", "그거 아니고", "왜 그랬어", "그렇게 하지 마" |
| **불만 표현** | "이상한 짓 하지 마", "되돌려", "원래대로" |

**중단 후 행동**:
1. 현재까지의 작업 상태를 보고한다 (변경한 파일, 수정 내용).
2. **변명하지 않는다.** 상황만 보고한다.
3. 사용자의 다음 지시를 기다린다.

### 13.2 반복 실패 자동 정지

- 같은 접근 방식으로 **2회 실패**하면, 3번째 시도를 하기 전에 **사용자에게 방향을 확인**한다.
- "한 번 더 해보겠습니다"라고 하지 않는다. 대신:
  ```
  [반복 실패 보고]
  - 시도한 접근: (무엇을 했는지)
  - 실패 원인: (왜 실패했는지)
  - 다른 접근 제안: (대안이 있으면)
  다른 방향으로 진행할까요?
  ```

### 13.3 확신 없는 판단 정지

- 코드 수정의 영향이 **불확실**하면, 수정하기 전에 사용자에게 확인한다.
- "아마 괜찮을 것이다"는 이유가 아니다.
- 확실하지 않은 것: "이 수정이 다른 곳에 영향을 줄 수 있는지 확인이 필요합니다."

---

## 변경 이력

| 날짜 | 모델 버전 | 변경 내용 | 이유 |
|------|-----------|-----------|------|
| 2026-04-03 | Opus 4.6 | 기존 단일 문서를 라우터 + 3개 문서로 분리 | 구현 외 작업에 파이프라인이 불필요하게 강제됨 |
| 2026-04-06 | Opus 4.6 | 서브 파이프라인 3종(버그/기능/리팩토링) 도입, 필수 게이트 추가, 범위 통제/Git/긴급 정지 규칙 신설 | 인사이트 분석: 오진 38건, 범위 확장 7건+, 오해 32건 해소 |
| 2026-05-08 | Opus 4.7 | B1.5 "외부 큐레이션 (External Curation)" 절 신설 — 리서치 단계에서 WebSearch 1회 이상 의무, plan.md "생략 사유" 명시 시 escape | 30일 사용 분석에서 "4번 옵션의 부재"가 사각지대 — LLM 다수결 보정. TDAD 논문(arxiv 2603.17973)이 "절차 강제보다 맥락 제공이 효과적"이라는 결론을 학술적으로 뒷받침 |
| 2026-05-14 | Opus 4.7 (1M) | B1.5 외부 큐레이션을 Claude WebSearch + codex 큐레이션 통합으로 확장. B1.6 모델 교차 검증 + B1.7 research.md 작성 단계 신설. B2를 "방향성 점검 게이트"로 의미 확장(research.md 기반). B3에 codex plan 검토 의무, B5에 codex 테스트 검증 의무 추가. 동일 변경을 A/C 파이프라인에도 적용 (A1.5/A1.6/A1.7/A2 진단 검토/A3/A5, C1.5/C1.6/C1.7/C2/C3/C5). 5.7절 "codex 호출 시 보안 게이트 (패턴 기반)" 신설. 6.5절 "codex 테스트 검증" 신설. 11절 셀프체크에 codex 의무 항목 4개 추가. | 외부 큐레이션(사람 자료)만으로는 분석/판단 단계의 LLM 다수결 편향이 남음. 다른 LLM(codex/GPT-5.5)의 추론을 추가 신호원으로 도입. 본 작업 자체가 새 절차를 메타로 시험: plan.md를 codex로 메타 검토 → 8개 지적 중 6개 채택. 상세는 `docs/plans/2026-05-14/codex-cross-validation/` |
| 2026-06-05 | Opus 4.8 (1M) | usage report(한달) 반영: 1.2 위험 승격 + 소/중/대 문서강도 모델 / 1.4 4문서 강제→규모별 조건부 / 5.2 단일 변경 원칙(중간 검증 가능 단위) / **5.8 페이즈 게이트 신설**(중·대규모 master+phases 분리, 로컬테스트 게이트 vs B5 codex검증 역할분리, 자동롤백 금지) / 6.6 데이터·마이그레이션 특칙 신설 / 7.3 learned 규모별 조건부 | over-scoping(Claude측 1위 마찰)·문서 피로 동시 해결. report.html+codex 교차검증 3회. 추가(게이트)와 감축(소규모 경량)을 한 모델로 통합. 상세는 `docs/plans/2026-06-05/usage-report-개선반영/` |
| 2026-06-08 | Opus 4.8 (1M) | 멀티워커 설계안(08) 반영: 1.2 규모 사용자 오버라이드+위험 하한 게이트(D6) / 5.9 코드리뷰 게이트 X4.5(D3, spec compliance) / 6.7 테스트 설계 분리 X4-T(D2, impl diff 미열람) / B·A·C 다이어그램에 X4-T·X4.5 추가 / B2·A2·C2 게이트에 멀티워커 캐스팅 확정 항목(D4) | 코드리뷰 가이드레일의 역할 분리를 구현 파이프라인으로 확장. 생성≠검증·구현≠테스트설계·리뷰 전용 단계 신설. 페르소나 캐스팅은 `orchestration-agent.md` 12절. 상세 `docs/08-멀티워커-오케스트레이션-설계안.md` |
