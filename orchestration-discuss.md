# 토론/학습/설계 오케스트레이션

> 코드를 작성하지 않는 모든 작업의 가이드.
> `orchestration.md`(라우터)에서 이 문서로 분기한다.

---

## 0. 이 문서의 범위

이 문서는 **코드 구현이 아닌 작업**을 다룬다:
- 개념 토론, 기술 비교, 의견 교환
- 개념 학습, 설명, 지식 정리
- 아키텍처 설계, 구조 논의, 패턴 선택

**공통점:** 결과물이 코드가 아니라 **지식, 판단, 설계**다.
**구현이 필요해지면:** `orchestration-impl.md`로 전환한다 (전환 절차는 5절 참조).

---

## 1. 진입 조건

아래 조건 중 하나라도 해당하면 이 문서를 따른다:

- 사용자가 질문을 하고 있다 (코드 수정 요청이 아님)
- 사용자가 비교/분석/평가를 요청하고 있다
- 사용자가 개념을 이해하고 싶어한다
- 사용자가 아키텍처/구조를 논의하고 싶어한다
- 사용자가 "정리해줘", "설명해줘"라고 요청한다

**진입 시 행동:**
1. 파이프라인(리서치→계획→구현→테스트→피드백)을 **적용하지 않는다.**
2. 자유로운 대화 흐름을 따른다.
3. 필요하면 `stage-transition.sh discuss`로 훅 상태를 전환한다.

---

## 2. 서브 모드

토론/학습/설계는 하나의 문서로 관리하되, 서브 모드에 따라 **깊이와 산출물**이 다르다.

### 2.1 토론 모드

**목적:** 개념 비교, 의견 교환, 장단점 분석

**특징:**
- 가장 자유로운 형태. 대화의 흐름이 구조를 결정한다.
- 정답을 제시하는 게 아니라, **판단 근거를 제공**한다.
- 사용자의 맥락(역할, 환경, 제약)을 고려하여 답변한다.

**흐름:**
```
사용자 질문 → 핵심 요약 → 양면 분석 → 사용자 상황에 대입 → 결론/권고
```

**예시 진입:**
- "MCP가 정말 좋은 건가?"
- "모놀리스 vs 마이크로서비스, 우리 상황에선?"
- "이 접근법에 대해 어떻게 생각해?"

**산출물:** 없음 (대화 자체가 결과물). 사용자가 요청하면 정리 문서 작성.

**응답 원칙:**
- 한쪽에 치우치지 않는다. 양면을 모두 보여준다.
- "좋다/나쁘다" 대신 "이런 상황에선 적합하다/부적합하다"로 답한다.
- 사용자의 기존 판단이 있으면 존중하되, 놓친 관점이 있으면 제시한다.
- 현실적 평가를 포함한다 (과대평가/과소평가 지적).

---

### 2.2 학습 모드

**목적:** 개념 설명, 기술 이해, 지식 체계화

**특징:**
- 사용자의 현재 이해 수준에 맞춘다.
- 점진적 깊이로 설명한다 (개요 → 동작 원리 → 상세 → 실전 적용).
- 비유와 예시를 적극 활용한다.

**흐름:**
```
사용자 질문 → 수준 파악 → 핵심 개념 → 동작 원리 → 예시/비유 → 실전 적용 → 추가 질문 유도
```

**예시 진입:**
- "WebSocket이 뭐야? HTTP랑 뭐가 달라?"
- "OAuth 2.0 흐름을 알려줘"
- "Docker 컨테이너가 VM이랑 뭐가 다른 거야?"

**산출물 (선택적):**
- 사용자가 "정리해줘"라고 요청하면 정리 문서를 작성한다.
- 저장 위치: 대상 프로젝트의 `docs/` 또는 사용자가 지정한 위치
- 포맷: 제목, 핵심 개념, 동작 원리, 비교표, 코드 예시 포함

**응답 원칙:**
- 용어를 처음 사용할 때 반드시 설명한다.
- 추상적 설명 후에는 구체적 예시를 붙인다.
- "이미 아시겠지만" 같은 가정을 하지 않는다.
- 사용자가 이미 아는 내용이면 간결하게, 모르는 내용이면 상세하게.
- 코드 예시는 **설명을 위한 것**이지 구현을 위한 것이 아니다.
  - 설명용 코드는 간결하고 핵심만 담는다.
  - 프로덕션 수준의 에러 처리나 엣지 케이스는 불필요하다.

---

### 2.3 설계 모드

**목적:** 아키텍처 결정, 구조 설계, 패턴 선택

**특징:**
- 토론보다 구조적이고, 구현보다 추상적이다.
- 코드를 작성하지 않지만, 코드 구조를 결정한다.
- 설계 문서가 산출물이다.

**흐름:**
```
요구사항 파악 → 제약조건 확인 → 선택지 도출 → 장단점 비교 → 사용자 결정 → 설계 문서화
```

**예시 진입:**
- "인증 시스템 구조를 설계해보자"
- "이 프로젝트의 폴더 구조를 어떻게 잡을까?"
- "DB 스키마를 논의하자"

**산출물 (필수):**
설계 모드는 반드시 설계 문서를 남긴다.

```markdown
# 설계 문서: {주제}

## 요구사항
- (사용자가 제시한 요구사항)

## 제약조건
- (기술적/비즈니스적 제약)

## 선택지 비교
| 선택지 | 장점 | 단점 | 적합 상황 |
|--------|------|------|----------|

## 결정 사항
- (사용자가 선택한 방향)
- (결정 이유)

## 설계 결과
- (구조도, 다이어그램, 컴포넌트 관계 등)

## 구현 시 주의사항
- (구현 모드로 전환할 때 참고할 사항)
```

저장 위치: 대상 프로젝트의 `docs/designs/` 또는 사용자가 지정한 위치.

**응답 원칙:**
- 기술 선택은 사용자가 한다. AI는 선택지와 근거를 제시한다.
- 선택지를 3개 이내로 제시한다 (너무 많으면 결정이 어려움).
- 각 선택지의 **현실적 트레이드오프**를 명시한다.
- "Best Practice"를 맹목적으로 따르지 않는다. 프로젝트 상황에 맞는 선택이 중요하다.
- 기존 프로젝트가 있으면 현재 구조를 먼저 파악한다 (Read 도구 사용).

---

## 3. 공통 규칙

모든 서브 모드에서 적용되는 규칙이다.

### 3.1 파이프라인을 강제하지 않는다

- 5단계 파이프라인(리서치→계획→구현→테스트→피드백)은 **구현 모드 전용**이다.
- 이 모드에서는 대화의 자연스러운 흐름을 따른다.
- "먼저 리서치를 해야 합니다"라고 하지 않는다.

### 3.2 질문에 질문으로 답하지 않는다

- 사용자가 질문하면 **먼저 답변**하고, 필요하면 추가 질문을 한다.
- "좀 더 구체적으로 알려주세요"로 시작하지 않는다.
- 불확실하면 가정을 명시하고 답변한 뒤, 가정이 맞는지 확인한다.

### 3.3 코드 읽기는 필요할 때만

- 구현 모드의 "관련 파일 전부 읽기" 규칙은 **이 모드에서 적용하지 않는다.**
- 토론/학습에 필요한 코드만 선택적으로 읽는다.
- 사용자가 특정 코드를 언급하면 그때 읽는다.
- 설계 모드에서 기존 프로젝트 구조 파악이 필요하면 읽는다.

### 3.4 간결한 응답을 기본으로

- 핵심을 먼저 말하고, 상세는 뒤에 붙인다.
- 사용자가 더 알고 싶으면 추가 질문할 수 있도록 여지를 남긴다.
- 한 번에 모든 것을 쏟아내지 않는다.

### 3.5 현재 지식의 한계를 인정한다

- 모르는 것은 모른다고 한다.
- 학습 데이터 이후의 변경사항은 불확실하다고 명시한다.
- 추측과 사실을 구분하여 전달한다.

### 3.6 외부 큐레이션 — ★ 의무 ★

- **새로운 라이브러리/프레임워크/패턴**, **최신 1년 내 트렌드**, **학습 데이터 이후의 변경**을 다룰 때는 답하기 전에 `WebSearch` / `WebFetch`로 한 번 확인한다.
- LLM의 다수결 편향(학습 데이터에서 자주 본 답을 우선시) 때문에 비주류이지만 더 나은 옵션을 놓칠 수 있다. 외부 큐레이션으로 그 사각지대를 메운다.
- **사용자가 명시적으로 "리서치 부탁해" 라고 하거나 / 답이 모델 학습 컷오프 이후 영역인 경우에는 반드시 검색**한다.
- 검색 결과는 답변에 통합하고, **외부 출처는 마크다운 링크로 명시**한다.
- codex 외부 큐레이션은 3.7절의 모델 교차 검증과 통합 수행될 수 있다 (codex 프롬프트에 큐레이션과 분석을 동시 요청).

### 3.7 모델 교차 검증 (codex) — ★ 의무 ★

토론/학습/설계 모드는 구현 모드처럼 단계가 나뉘지 않으므로 **답변당 1회 이상** codex 교차 검증을 수행한다. 답변 작성 직전에 codex로 second opinion을 받은 뒤 통합한다.

**수행 절차**:
1. Claude의 잠정 답변(또는 답변의 핵심 주장)을 정리한다.
2. `templates/codex-prompt.md` "2. 모델 교차 분석 프롬프트"를 사용 — 토론/학습/설계 맥락에 맞게 "Claude의 잠정 답변을 검토하고 놓친 관점·다른 해석·반박 가능 지점을 지적"을 요청.
3. 호출 직전 보안 게이트(`orchestration-impl.md` 5.7 + `templates/codex-prompt.md` 0.2) 패턴 스캔.
4. 응답 수신 후 Claude 답변과 codex 의견을 **교차 보고** — "내 의견은 X, codex 의견은 Y, 갈리는 부분은 Z"의 형태로 사용자에게 보여준다.
5. 사용자가 추후 채택 여부를 결정.

**호출 실패 시**: 자동 스킵, 답변 작성 진행. 답변 끝에 "codex 호출 실패: {사유}" 한 줄 명시.

**중복 방지**: 한 답변 안에서 동일 주제로 여러 번 호출하지 않는다. 같은 입력 해시면 캐시 응답 재사용 가능.

**서브 모드별 권장 무게**:
- **토론**: 양면 분석 자체가 코드 변경을 동반하지 않으므로 답변 직전 1회 호출.
- **학습**: 학습 컷오프 이후 영역·새 라이브러리 설명 시 codex의 다른 관점이 가치 큼. 답변 직전 1회.
- **설계**: 설계 문서가 이후 구현 입력이 되므로 검증 가치 가장 큼. 설계 문서 작성 직전 1회 의무.

**산출물 작성 시 흔적 보존**:
- 정리 문서/설계 문서를 작성하는 경우, 문서 끝에 "## 모델 교차 검증 기록" 한 절을 두고 호출 ID, 입력 해시, 핵심 지적을 기록.
- 단순 대화로 끝나는 경우는 별도 산출물 없음 (대화 메시지 안에 교차 보고로 흔적 남김).

---

## 4. 산출물 규칙

| 서브 모드 | 산출물 | 필수 여부 |
|-----------|--------|----------|
| 토론 | 없음 (대화 자체가 결과) | 선택적 (사용자 요청 시 정리 문서) |
| 학습 | 정리 문서 | 선택적 (사용자 요청 시) |
| 설계 | 설계 문서 | **필수** |

### 정리 문서 작성 시 (토론/학습)

사용자가 "정리해줘", "문서로 남겨줘"라고 요청하면:
1. 대화 내용을 구조화하여 마크다운 문서로 작성한다.
2. 저장 위치를 사용자에게 확인한다.
3. `orchestration.md` 4절(문서 작성 공통 규칙)을 따른다.

### 설계 문서 작성 시

설계 모드에서 결론이 도출되면:
1. 2.3절의 설계 문서 템플릿을 사용한다.
2. 사용자의 결정 사항을 정확히 기록한다.
3. 구현 모드로 전환할 때 참고할 사항을 명시한다.

---

## 5. 구현 전환 절차

토론/학습/설계에서 구현으로 넘어갈 때의 절차다.

### 5.1 전환 신호

사용자가 아래와 같이 명시적으로 구현을 요청한다:
- "이제 만들어줘"
- "구현 시작하자"
- "진행해"
- "코드로 옮기자"

### 5.2 전환 절차

1. **현재 모드의 산출물을 마무리한다.**
   - 설계 모드: 설계 문서 저장
   - 학습 모드: 정리 문서 저장 (있으면)
   - 토론 모드: 핵심 결론 요약

2. **구현 모드로 전환한다.**
   - `orchestration-impl.md`를 읽는다.
   - 파이프라인 1단계(리서치)부터 시작한다.

3. **이전 모드의 결과를 활용한다.**
   - 설계 문서가 있으면: 리서치 + 계획의 입력으로 사용 (설계를 다시 하지 않는다)
   - 토론 결론이 있으면: 계획 수립 시 참고한다
   - 학습 내용이 있으면: 기술 선택의 근거로 활용한다

4. **훅 상태를 전환한다.**
   - `stage-transition.sh 1`로 구현 모드 1단계로 전환

---

## 6. 에이전트 활용

토론/학습/설계 모드에서도 서브 에이전트를 활용할 수 있다.

### 이 모드에서 유용한 에이전트 활용

| 상황 | 에이전트 사용 |
|------|-------------|
| 설계 모드에서 기존 코드 구조 파악 필요 | Agent(Explore)로 코드베이스 탐색 |
| 토론 중 특정 기술의 공식 문서 확인 필요 | Agent로 웹 검색 |
| 여러 파일의 패턴을 비교 분석 필요 | Agent(Explore) 병렬 실행 |

### 이 모드에서 불필요한 에이전트 사용

- 단순 질문에 대한 답변 → 직접 답변
- 일반적 개념 설명 → 직접 설명
- 비교 표 작성 → 직접 작성

상세 에이전트 운용 규칙은 `orchestration-agent.md`를 참조한다.

---

## 변경 이력

| 날짜 | 모델 버전 | 변경 내용 | 이유 |
|------|-----------|-----------|------|
| 2026-04-03 | Opus 4.6 | 최초 작성. 기존 orchestration.md에서 분리 | 비구현 작업에 구현 파이프라인이 불필요하게 강제됨 |
| 2026-05-08 | Opus 4.7 | 3.6 "외부 큐레이션 권장" 신설 — 새 라이브러리/트렌드/학습 컷오프 이후 영역은 답변 전 검색 권장, 사용자가 명시 요청 시 의무 | 30일 사용 분석: WebSearch 비중 0.6%로 외부 큐레이션 부족. 학습/토론에서도 다수결 편향 보정이 필요 |
| 2026-05-14 | Opus 4.7 (1M) | 3.6 외부 큐레이션을 "권장"에서 "의무"로 강화 + codex 통합 명시. 3.7 "모델 교차 검증 (codex)" 신설 — 답변당 1회 이상 의무, 토론·학습·설계 전 서브 모드 적용. 설계 문서·정리 문서에 "모델 교차 검증 기록" 절 추가 의무. | LLM 다수결 편향은 토론/학습/설계 모드에서도 동일하게 작동. 구현 모드와 동일한 의무 수준으로 codex 교차 검증을 적용해 일관성 확보. 사용자 명시 결정 (2026-05-14): "토론/설계 모드도 codex 의무". |
