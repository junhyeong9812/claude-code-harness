# 최종 검증 요청: LLM 코딩 하네스 "차원 지도" — 머지 판정

당신은 독립 최종 검증자다. 계획 검토(이전 라운드) 지적이 반영된 최종본이다.
판정하라: 머지 가능 / 조건부(고칠 것 명시) / 불가. 새 지적은 집행 공백·모순·회귀 위험에 한정하라.

## 이전 라운드 지적 → 반영 내역
1. light 활성 집행 공백(최중요) → light도 stakes 포함·이항 미확정은 보수값·표 아래 "light 상세" 기록(P/I 답+V 계획)·검증 종료 전 재판정 의무
2. 소유 경계(5 동시성 너무 넓음 등) → 5를 "같은 리소스에 중복·동시 실행이 실제 가능한 write"로 한정, 9를 "내가 제어하지 않는 의존성"으로 명시
3. 낮음↑ 표기 모호 → "기본 하한 낮음, 괄호 조건 충족 시 그 값" 정의 + 전 차원 비활성=낮음 명시
4. 팩 "중복 점검 금지" 위험 → "팩은 기본 차원을 끄는 근거가 아니다"로 교체 + 연동 규칙(F7 PII→11, I3→11)
5. 공급망 누락 → 16 활성 신호에 의존성 도입·버전 변경 + 16→11 light 연동
6. 일회성 대량 보정 스크립트 → 배치팩 detector에 추가
7. 트리아지 전용 표 분리(같은 파일 내) → 트리아지 표를 앞으로, P/I/V는 뒤 §단계 질문으로 재구성 (81줄)
8. 경량 경로 폐지 완화 → 전 차원 비활성 자명 작업(오타·주석 수준)의 1행 축약 허용
추가로 신선 컨텍스트 에이전트(다른 LLM 세션) 시나리오 검증이 8건을 더 잡아 반영: 이항 미확정 보수값 / 공집합 하한=낮음 / light 기록 위치·V는 계획 / 누적 승격="적용값 중간" 3개+·높음이면 무승격 / 영향면 보정은 최종 하한에 적용·증거는 차원 귀속 / 변경 이력은 기록용·본문 정본 / 팩 차원 ID 주석 / 게이트 시점 증거 수준(훑기·grep 충분, 추정 비활성 금지)

## 검증 질문
1. 위 반영으로 light·stakes 도출·트리아지 집행에 남은 구멍이 있는가?
2. core.md 배선(아래 발췌)과 dimensions.md 사이 모순·중복 기재(단일 출처 위반)가 있는가?
3. 이 체계가 6개월 운영에서 실패한다면 이제 무엇이 가장 약한 고리인가? (이전 답변: boilerplate화·등급 인플레·소유권 부재 — 이 중 미해소분 포함)
4. 머지 판정: 가능/조건부/불가 + 조건.

## core.md 배선 발췌
- §1: "칸 2·6은 dimensions.md 트리아지로 채운다 — 전 stakes 의무: 조건부 차원 14개 전수 판정(행 형식 판정/근거/본 파일·심볼/불확실성), stakes는 활성 차원에서 하한 도출. 행 형식·light 규칙·도출 규칙·확장팩은 dimensions.md가 단일 출처."
- §3.5: "경량 경로 폐지 (2026-06-10 사용자 결정): 모든 정의됨 작업은 task.md를 작성한다. 전 차원 비활성이 자명한 작업(오타·주석 수준)의 트리아지 1행 축약만 허용 — 형식은 dimensions.md 사용법을 따른다."
- §7 표: "dimensions.md | 정의 게이트 진입 시 — 모든 정의됨 작업 (칸2·칸6 트리아지). 표면이 배치/프론트/인프라/일회성 대량 보정이면 dimensions-*.md 팩 추가"
- templates/task.md §1: 14행 트리아지 표 + light 상세 목록 + §3 질문 보정 기록 표

---

# 최종본 (dimensions.md)

# dimensions.md — 개발 차원 지도 (트리아지 + 단계별 렌즈)

> 로딩 트리거: 정의 게이트 진입 시 (core §7). 절차 규칙은 core.md, **차원 내용은 이 문서가 단일 출처.**
> 구조: §트리아지 표(판정용 — 정의 시 이것만 보면 됨) → §단계 질문(활성 차원만) → §보정·§확장팩·§정비.
> 개정은 월 1회 측정 리뷰의 승격 절차로만. 인간용 자매 문서: claude_study `docs/17`(승격분만 반영).

## 사용법 — 트리아지 (모든 정의됨 작업, 전 stakes)

1. 아래 표의 14차원을 **전수** 판정해 task.md §1 트리아지 표에 기록. 행 형식: `판정(활성/light/비활성) / 근거 / 본 파일·심볼 / 불확실성`.
2. **비활성에도 증거를 인용한다** (변경 파일·심볼·diff 범위) — "해당 없음" 한마디 금지.
   단, **전 차원 비활성이 자명한 작업**(오타·주석·공백 수준)은 1행 축약 가능: `전 차원 비활성 — 증거: diff 범위 인용`. 하나라도 활성 의심이면 전개.
3. **불확실하면 비활성 금지** → light 활성. **light 규칙 (집행 조건)**:
   - light도 **stakes 하한 계산에 포함**한다 (증거로 비활성 전환해야 제외). 이항 표기 조건이 미확정이면 보수 쪽(높은 값)으로.
   - light 행은 트리아지 표에는 판정·근거만 적고, 표 아래 **"light 상세"** 목록에 `불확실한 이유 + 확인할 증거 + P/I 답 + V 계획`을 기록. "해당 없음" 단독 금지 — 질문이 안 맞으면 §보정 루프로.
   - **검증 단계 종료 전 light는 활성/비활성 중 하나로 재판정**한다 — light인 채 작업을 끝내지 않는다.
6. **증거 수준 (게이트 시점)**: 변경 대상 파일·디렉토리의 확인(대상 파일 훑기·grep·ls)이면 충분 — 호출처 전체 실독은 정의됨 이후 구현 리서치(core §2)의 몫. 단 **추정만으로 비활성 판정 금지**.
4. 활성/light 차원의 질문이 작업에 안 맞으면(부적합) §보정 루프로 — 임의로 질문을 바꾸지 않는다.
5. 작업 표면이 배치/프론트엔드/인프라/일회성 대량 보정에 닿으면 확장팩 추가 로드 (§확장팩). **팩은 기본 차원을 끄는 근거가 아니다.**

## stakes 도출 — 칸6 = max(아래 1~3, 낯섦·모호성 판단)

1. **하한**: 활성+light 차원의 stakes 표기 최대값. 표기 `낮음↑` = 기본 하한 낮음, 괄호 조건 충족 시 그 값. 이항 표기(public=높음·내부=중간 류)의 조건 미확정은 보수 쪽. **활성+light 0개면 하한 = 낮음.**
2. **영향면 보정**: 1의 하한(최종값)에 적용 — production 코드·공개 표면·돈·PII·불가역에 닿으면 그대로, 전부 비켜가면 한 단계 하향 가능. **면제에도 증거 의무**, 증거는 그 하한을 만든 차원에 귀속해 기록 (예: "#3은 테스트 fixture만 — 경로 인용").
3. **누적 승격**: 적용값이 **중간**인 차원 3개+ 동시 활성 → 한 단계 승격 검토 (blast radius). 이미 높음이면 승격 없음.
4. 낯섦(처음 쓰는 lib·패턴·배포)·모호성(요구 불명확)은 차원이 못 잡는다 — 별도 판단 후 max.

## 트리아지 표 (ID = 원 18번호)

| # | 차원 | T: 이 변경이 …는가 | 활성 신호 (경로 힌트) | 비활성 신호 | stakes |
|---|------|--------------------|----------------------|------------|--------|
| 2 | 입력 검증 | 외부에서 온 값(입력·요청·파일·헤더)을 새로 받거나 검증 경로를 바꾸 | 파라미터/DTO 신설·변경 · 업로드 · 외부 파싱 (controller·dto·validator) | 내부 시그니처만 · 기검증 값 전달 | 낮음↑(공개 표면=중간) |
| 3 | 권한 경계 | 인증·인가·소유권·tenant 경계를 지나는 경로를 만들거나 바꾸 | 신규 endpoint · role/ownership check · admin · 토큰 (auth·security·middleware·policy) | 권한 검사 이후 내부 로직만 · fixture만(증거) | 높음(비production 증명=중간) |
| 4 | 데이터 정합성 | 데이터를 쓰거나(write) 상태를 바꾸 | insert/update/delete · 상태 전이 · 트랜잭션 경계 (service·repository·migration) | 순수 조회 · 표현 계층만 | 중간(돈·불가역·마이그레이션=높음) |
| 5 | 동시성 | **같은 리소스에 중복·동시 실행이 실제 가능한** write 경로를 만들거나 바꾸 | 재고/잔액/카운터 · 연타·재시도 유발 endpoint · 다중 인스턴스 잡 (queue·scheduler·lock) | 단일 실행 보장된 경로 · 불변 데이터 조회 | 중간(돈이 움직이는 write=높음) |
| 6 | 예외 처리 | 실패 가능 동작(외부 IO·write·API 경계·job·타임아웃·상태 변경)을 만들거나 바꾸 | 외부 호출 · write · 신규 endpoint · 에러 매핑 (exception·handler·advice·client) | 순수 리네임 · 계산식 정리 | 낮음↑(실패가 사용자·데이터에 전파=중간) |
| 8 | 성능 | 쿼리·데이터량·호출량 특성을 바꾸 | 신규 쿼리/조인 · 루프 내 호출 · 페이징·검색 · 대량 처리 (repository·query·batch) | 데이터량 특성 불변 · 상수만 | 낮음↑(핵심 경로·대량=중간) |
| 9 | 장애 복구 | **내가 제어하지 않는 의존성**(외부 API·queue·타 서비스)의 실패가 전파되는 경로를 만들거나 바꾸 | 외부 호출 · MQ 발행/소비 · webhook (client·consumer·retry) | 단일 프로세스 내 동기 로직만 | 중간(불가역 외부 효과=높음) |
| 10 | 운영 가능성 | 배포 후 관찰·복구가 필요한 동작을 추가·변경하 | 신규 기능 경로 · 배치 · 설정값 · 로그 의미 변경 (config·logging·metrics) | 기존 관측 범위 안 미세 수정 | 낮음↑(수동 복구 필요 기능=중간) |
| 11 | 보안 | 공격 표면(입력 파싱·토큰·파일·secret·서버 간 통신·로그)을 만들거나 바꾸 | 토큰/세션 · 업로드 · secret · CORS/CSRF · PII 취급 (security·filter·config·env) | 공격자 도달 불가 내부 로직(증명) | 높음(내부 전용·비production 증명=중간) |
| 12 | API 계약 | 외부가 의존하는 계약(URL·스키마·이벤트·에러 포맷)을 만들거나 바꾸 | 신규/변경 endpoint · 응답 필드 · 이벤트 스키마 (controller·dto·openapi) | 계약 동일이 증명되는 내부 변경 | public=높음 · 내부=중간 |
| 14 | 도메인 규칙 | 상태 전이·금액/수량 계산·정책 조건을 만들거나 바꾸 | 계산식 · 상태 머신 · 정책 분기 · 반올림/통화/시간대 (domain·policy·calculator) | 도메인 의미 불변의 기술 변경 | 중간(돈·법적 의무=높음, **보안·권한 없어도**) |
| 15 | 데이터 모델링 | 스키마·관계·제약·인덱스를 만들거나 바꾸 | 테이블/컬럼 · 마이그레이션 · unique/FK · 인덱스 (entity·migration·ddl) | 모델 불변의 조회·로직 변경 | 마이그레이션·기존 데이터=높음 · 신규 모델=중간 |
| 16 | 비용 | 자원 소비 구조(서버·DB·호출량·저장량)나 복잡도를 유의미하게 바꾸 | 유료 API · 캐시/인프라 추가 · 적재 증가 · **의존성 도입·버전 변경(공급망)** (client·infra·build) | 자원 특성 불변의 로직 변경 | 낮음↑(호출·저장량 단위 증가=중간) |
| 17 | 사용자/소비자 가시성 | 사용자·API client·운영자가 보는 동작(응답·메시지·지연·상태)을 바꾸 | 응답 내용/형식 · 지연 증가 · 에러 메시지 · 알림 (controller·view·message) | 가시 동작 동일이 증명되는 내부 변경 | 낮음↑(신뢰 훼손 가능=중간) |

**자동 연동**: 3 활성 → 11 light · 16 의존성 변경 활성 → 11 light · F7 PII → 11 활성 · I3 secrets → 11 활성 (F·I는 팩 차원 — 해당 팩 로드 시만 의미). 한 변경이 여러 차원을 켜는 건 정상 — 각 차원은 자기 질문만 소유한다.

## 단계 질문 (활성·light 차원만 펼침 · P=계획 I=구현 V=검증)

- **2**: P 검증 규칙(필수·길이·형식·범위·enum)이 계약에 명시됐나? 프론트/백 분리는? | I **백엔드가 최종 방어선** — 어느 층에서 막나? 인젝션 지점은? | V 빈 값·경계값·잘못된 enum·인젝션 테스트?
- **3**: P 인증(누구)/인가(되는가)/소유권(그 사람 것인가) 3단이 각각 어디서? | I 권한 없는 사용자가 도달하면? 우회 경로는? | V 권한별 결과·남의 리소스 접근 테스트?
- **4**: P 트랜잭션 범위? 실패 시 롤백 범위? 삭제는 물리/논리? | I 상태 전이 검증이 코드에 있나? 중복 요청 방어는? | V **데이터 상태를 직접 검증**(호출 성공만 말고)? edge(빈·중복·stale)?
- **5**: P 방어 선택 — unique 제약/낙관락/비관락/분산락/idempotency key/상태 기반? | I 락 범위·데드락은? 멱등 키 생성 위치는? | V 동시·중복 요청 재현 테스트?
- **6**: P 에러 코드·메시지 정책이 기존 전략과 일관한가? | I 복구/재시도/알림 구분? 민감정보가 응답에 새나? | V 실패 의미론마다 예외 경로 테스트 1개+?
- **8**: P 인덱스 타는 조건? 페이징·count 전략? 캐시? | I N+1? 메모리 전량 적재? | V 데이터 10배에 버티나? 쿼리 수 실측?
- **9**: P 상대가 느림/다운/이상 응답일 때 각각? retry·timeout·fallback·DLQ·보상 중 무엇? | I 재시도가 중복 실행을 만드나(idempotency)? checkpoint·재처리는? | V 의존 실패 주입 플로우 디버깅? 실패 기록 위치?
- **10**: P 설정을 코드 수정 없이? 롤백·기능 끄기? | I requestId·traceId·사용자 id 로그? 실패 알림? | V 장애 시 따라 할 runbook·보정 쿼리·재처리 도구?
- **11**: P 하위 영역 — secrets/암호화/세션·토큰/PII(최소수집·보존·마스킹)/감사 로그 중 해당은? (참조: OWASP ASVS) | I 인젝션·XSS·CSRF 방어? 로그에 개인정보·secret? | V 악의 입력·권한 우회 테스트?
- **12**: P 하위 호환? 리소스 중심 URL·Method·에러 포맷 통일? | I 엔티티 직노출? 과다 응답? | V 기존 클라이언트가 깨지나? (최소 안전선 contract 항목 연결)
- **14**: P 불변식을 한 문장으로? 규칙이 한 곳에 사나? | I 경계값(0원·음수·환불 후 재환불)에서 유지되나? | V 불변식을 직접 검증하는 테스트?
- **15**: P 데이터 주인? 관계? 정규화? 이력? 삭제 정책? (unique = 동시성 방어 첫 줄 — 5 연동) | I 제약이 DB에 있나, 코드에만? | V 마이그레이션 record-level 검증(core §6.3) — count·sample·orphan?
- **16**: P 더 싸고 단순한 대안? 복잡도(학습·변경 비용 포함)를 감당할 가치? | V 호출량·저장량 변화를 추정해 기록했나?
- **17**: P 처리 중·성공·실패에 각각 무엇이 보이나? 메시지가 다음 행동을 알려주나? | V 가시 동작을 실제로 확인(테스트 통과 말고)?

## 질문 보정 루프

- **부적합**(질문이 이 도메인에 안 맞음 / 어떤 질문도 못 잡는 실패모드 발견) ≠ **비활성**(트리아지의 정상 경로).
- 부적합 발견 → 대체/신규 질문을 사용자에게 제안 → 결정을 task.md "질문 보정 기록"에 `질문 ID / 사유 / 제안 / 결정`으로 기록. 정본은 그 자리에서 고치지 않는다.
- 정본 반영은 월 1회 측정 리뷰에서 승격 기준 통과분만: ① 서로 다른 작업 2회+ 재등장 ② 질문 공백이 실제 결함으로 연결.

## 확장팩 (surface detector — 해당 시 추가 로드, 기본 14차원은 그대로 유지)

| 표면 | 감지 질문 | 팩 |
|------|----------|-----|
| 배치/데이터 파이프라인 | 스케줄 실행·대량 데이터 이동·ETL/집계 잡, **또는 일회성 대량 데이터 수정·재처리 스크립트**인가? | `dimensions-batch.md` |
| 프론트엔드 | 브라우저/앱에서 실행되는 UI 코드인가? | `dimensions-frontend.md` |
| 인프라 | IaC·배포 설정·네트워크·클라우드 리소스 변경인가? | `dimensions-infra.md` |

## 정비 규율

- 상한: 본체 ≤200줄 · 팩 ≤60줄. **색인이지 교과서가 아니다** — 예시·안티패턴·표준 본문 복사 금지, 참조 ID만 (OWASP ASVS · 12-factor · AWS Well-Architected · Google SRE PRR).
- 트리아지 오판(비활성 차원이 뒤늦게 등장)은 measurement-log "검증이 잡은 결함" 열에 `트리아지 오판(차원n·발견단계)`로 기입 — **이 데이터만이 개정 근거** (core §0-3).
