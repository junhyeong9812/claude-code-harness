# 하네스 사용 실측 리서치 — 종합 (2026-08-27, v4 배포 07-21 이후 5주)

> 입력: w1-docs-ledger.md(문서 ledger 222 작업폴더/202 log) · w2/w3/w4-jsonl-*.md(대화 전사 110세션, 사이드체인 1,039개는 개수만) · 메인 교차확인 5건(PR 오염 커밋 a7863daa7ed·git-guard grep 오탐 원문·jun-bank docs 독립 repo·8c8e5f1d SPEC 재사용·review.md:14 packet 정의). 워커 모델 = Opus 5기.
> 이 문서는 **리스트업**이다. 개선 결정은 사용자 대화로.

## 1. 리뷰 효과 — 숫자

| 지표 | 값 | 출처·한계 |
|---|---|---|
| 듀얼(codex+Opus) 실제 실행 작업 | 118/202 (58%) | W1 §1-A |
| 리뷰 형태 분포 | 루프형 18 · 듀얼1패스 102 · codex단독 23 · 셀프 20 · 없음 39 | W1 |
| codex 호출 | ≈509회 (W2 267 + W4 188 + W3 54), 실패 ≈18 (3.5%) — timeout 8·503 2·auto-mode 분류기 5·sleep 폴링 차단 3 | W2/W3/W4 |
| Opus 워커 호출 | ≈772회, 도구 레벨 미회수 0 | 〃 |
| **codex 채택률** | ledger 75개 실수치 **≈70%**(250→175) · 단독 지적만 58% | W1 §2-A, W4 |
| **Opus 채택률** | **≈64%**(380→245) · 단독 지적만 37% | 〃 |
| 양쪽 공통 지적 채택률 | **88%** — 교차 일치가 가장 강한 신호 | W4 |
| 감사·post-fix 재점검 채택률 | ~90% (가장 높음) | W1 |
| 지적 유형 | 실결함 55% · 그린위장/테스트품질 15% · 문서동기화 12% · 오탐 10% · **packet 결함 오탐 5%** · 범위밖 3% | W1 §1-C (표본) |
| **독(害) 케이스** | **24/202 작업 (11.9%)** — 반영이 새 결함 5 · 정지규칙 churn 5 · 순가치 0 5 · 도구실패 시간손실 5 · 오탐 채택으로 산출물 악화 **1**(resume 메일OTP 날조) | W1 §2-C |
| 정지 규칙 발동 | 11~14회 (workbench 8, jun-bank 4, harness 2) | W1/W2 |

**해석**: 오탐(10%)은 대부분 기각으로 걸러져 코드에 안 들어갔다(markCloud 4/4 기각). 리뷰의 해악은 오탐이 아니라 **①채택된 수정이 새 결함을 만드는 것**(D2~D6, post-fix 재점검이 잡음) **②같은 표면 반복으로 인한 churn**(memory-optimization −191줄 폐기·12%p 손실, prompt-refine 감사 5회) **③설계 문서에서 종료조건 부재**(jun-bank R10+·resume 5라운드 비단조)였다. 리뷰 최고 가치 영역은 **그린 위장 적발**(W1 §3-I 9건).

## 2. 문제 리스트업 (빈도·강도순)

### P1. 하네스 자체 결함 (훅이 사고를 만든 것) — 최우선
| # | 문제 | 실측 | 근거 |
|---|---|---|---|
| P1-1 | **`.prompt`/상태파일 사이드카가 repo에 유출** — capture-prompt가 조상 상태파일 못 찾으면 `$CWD/.claude/lazymode` 폴백 → Bash `cd` 따라 하위 디렉토리에 생성 → `git add -A`에 섞임 | upstream **PR #37157 커밋 a7863daa7ed에 2개 포함**(사용자 발견) · jun-bank/infra 8파일 유출 → filter-branch · `find /home/jun/project -name lazymode -type d` 다수 | W3 §B, W2 §B2, 메인 확인 |
| P1-2 | **docs가 git 루트인 저장소에서 게이트 교착** — `is_docs_exempt`가 루트 상대경로 `docs/` 요구 → spec/log 생성 자체가 L1 차단 | jun-bank/docs(독립 repo) 4세션 13회, **08-05·08-12·08-13 3회 재발**, 규명은 08-13 | W2 §A1, W1 §3-E, 메인 확인 |
| P1-3 | **문자열 패턴 오차단** — git-guard가 복합 명령 전체를 검사해 자기검증용 `grep 'claude\|anthropic\|codex'` 차단(2회, heredoc 연쇄실패) · attribution 가드 작업의 ledger 커밋 차단(2회) · `git rm --cached …lazymode…` 차단 · `.events` redirect 차단 · 스크래치 테스트 4회 | 총 ≈11회 | W3 §A, W2 §A3·B1 |
| P1-4 | **cwd 추종 오차단** — Bash persistent `cd` 후 하위 디렉토리 기준 상태 조회 → SPEC=0 거짓 차단 | harness 07-23 2회, workbench 07-21 1회 | W1 §3-E |
| P1-5 | **워크트리·multi-repo마다 상태 분리** — 같은 승인을 경로별 반복 기록(markCloud spec-approved 87/폴더 66, jun-bank 73/17), 세션 도중 상태 소실 1건 | W4 §A1·A2, W2 §B3 |
| P1-6 | **워커의 log.md Write가 task-mode-guard 리셋** → 구현 중단·재합의 | workbench codex-term, junhyeong9812 | W1 §3-E |
| P1-7 | **컴팩션 후 게이트 상태 리셋/재기록** 2건 | jun-bank 08-13, workbench 08-12 | W2 §D |

### P2. 게이트 설계의 실효 공백 (규칙은 있으나 안 걸림)
| # | 문제 | 실측 |
|---|---|---|
| P2-1 | **직전 SPEC=1 재사용** — spec 1개로 범위 밖 L1 장시간(8c8e5f1d: 3h·flyway V131/V132 포함 ~35회 쓰기, 새 폴더 없음). 훅 관측 불가(core가 자인한 한계)가 실제로 광범위 | W4 §A4, 메인 확인 |
| P2-2 | **Bash 경유 쓰기가 53%**(2,089 vs 1,857) — 극단 세션 298:8, 125:0. 하드 차단 표면이 사실상 없음 | W3 §I |
| P2-3 | **인터뷰 형해화** — L0성 문서(학습노트·맞춤법)에 게이트가 걸려 "AskUserQuestion 1회 → set-state 44초"로 의례화 | W2 §A2 |
| P2-4 | **lazy 실사용 0~10회**(개인 0, upstream OSS만 10) — 2택이 실질 1택. lazy에서도 PENDING_GATE가 "한 논리 diff의 2번째 Edit" 차단(Edit≠diff) | W2 §E, W3 §G, W4 §A5 |
| P2-5 | **DEBT 이원화** — `## 생략한 검증` 47회 vs `DEBT=1` 1회(markCloud) · `debt-clear` 실사용 ≈1회 · was-server crsv7/v8 해소기록 없음 → "차기 L1 빚 우선" 미발동 | W4 §A6, W1 §3-B |
| P2-6 | **긴급 판단 누락** — 배포 막힌 국면에 본작업급 절차 적용 → 4턴 질책·3회 인터럽트, 사용자가 직접 `urgent-fix-weight-protocol` 메모리 작성. 실제 긴급 진입은 5주간 3~4회 | W4 §C1 |
| P2-7 | **auto인데 턴을 끊는 습성** — "왜 이어서 쭉 안만들고 계속 검사를 받는거지" 등 3회(98a872b3) | W3 §F |
| P2-8 | 모드 답변이 자동 기록 안 됨 — L1 진입마다 set-state 수동(82회) | W4 §A3 |

### P3. 리뷰 프로세스
| # | 문제 | 실측 |
|---|---|---|
| P3-1 | **packet이 `git diff` 기반이라 untracked 파일 누락** → 완전 오탐 최소 5건, 11개 작업에서 관측 (review.md:14 정의) | W1 §3-D |
| P3-2 | **packet-only 격리가 Opus 오탐의 원인** — Opus 단독 채택 37%, 명시 오탐 전부 "실코드 미확인". 반대로 codex가 격리를 어겨 배포 블로커 잡은 사례(D24) | W4 §B1, W1 D24 |
| P3-3 | **수정이 새 결함**(D2~D6) — post-fix 재점검이 잡았으나 "리뷰가 오히려 손해" 자인 10건+ | W1, W2 §C1 |
| P3-4 | **정지 규칙 발동 전 비용** — 3라운드 후 −191줄 폐기, 감사 5회 소모 | W1 §3-H |
| P3-5 | **설계 문서 리뷰 종료조건 부재** — 08-06 분기 이전 R10+/5라운드 비단조("문서가 수렴하지 않고 흔들린다") | W2 §C4, W3 |
| P3-6 | **제안 문구를 사실로 반영 → 날조**(resume 메일OTP, 사용자 적발) | W1 D1 |
| P3-7 | codex 환경 마찰 반복 — auto-mode 분류기(bypass 플래그) 5회·sleep 폴링 3회·bwrap 샌드박스·10분 timeout 8회. 회수 실패 1건은 `review blocked` 안 타고 우회 | W2/W4 |
| P3-8 | 워커 헤드라인 증거 오전재 — "e2e 9/9" 재현 시 8/1 FAILED, 테스트 수 768 vs 717, "green" 부정확 | W1 §3-F |
| P3-9 | 같은 워크트리에 워커 2개 → 커밋 섞임("파일이 안 겹쳐도 커밋 도구가 겹친다") | W2 §D |

### P4. 사고 유형 (core §0.3 실측 귀속 재확인)
| 유형 | 건수(5주) | 대표 |
|---|---|---|
| **그린 위장 / 테스트 무효** | 20+ | run.sh --check exit code 폐기·stale XML, 스켈레톤 없는 박스 88/88 통과, 변하는 축 고정 테스트, weakref 테스트 수정 전에도 green |
| **silent failure** | 20+ | 헬스체크 6일 healthy 위장, compose volumes 무효→933GB 유실 직전, 500→200 기록, 부분결과 무음 리콜 손실 — **대체로 사용자가 먼저 발견** |
| **상태 오인** | 8+ | 재배포 후 컨테이너를 정본 오인, 로컬 .env를 운영 오인, push된 마이그레이션 제자리 수정→checksum, 모델 Fable↔Opus 혼동 2 |
| **날조·과대진술** | 25+ (resume/study 집중) | 손계산 기대값 반복 오류(Box 4건+), 없는 회사 "제출됨", 본문 없는 화살표, 근거 없는 수치가 제출본에 잔존, codex 문구 무검증 반영 |
| **외부 발행 사고** | 3 | PR #44~68 25건 attribution footer(사용자 적발) · DCO 누락 CI 실패 · PR 커밋 하네스 잔재 |
| **churn** | 문서 작업 집중 | 포트폴리오.md 34회, OTP 구현→삭제→복원 왕복 2회, risk_rank 1h13m에 폴더 5개 |

### P5. 잘 지켜진 것 (positive — 유지)
- spec+log 2파일 84~91%(미보유는 전부 L0 폴더) · 6칸 키워드 98% · 저장 위치=변경 프로젝트 준수
- 긴급 경로 절차 완전 준수 1건(log Write 선행→확인→emergency) · **운영 파괴 작업 무승인 실행 0건**(DROP/TRUNCATE/DELETE/ALTER/ES 전수)
- push 전건 ask 흐름(사용자 거부 5건 정상) · 커밋 메시지 검증출처 제거 준수 · 워커 도구레벨 미회수 0
- 정지 규칙은 발동해야 할 때 발동 · DEBT 미해소 시 완료선언 금지 이행(local-llm)

## 3. 판정 불가·한계
- 사이드체인 전사 1,039개 미열람(워커 내부 게이트 우회·자격증명 취급 미확인) · W1 log 52개 전문 미독 · 채택/기각은 메인 자기신고 기반(독립 재현 없음) · stakes별 집계 불가(미기재 다수) · `[구현 검증]` 중앙 대장 실파일 미확인 · 운영 자격증명 전사 평문 119회(사용자 제공분, §6 위반 아님 — packet 유입 리스크만 기록)
