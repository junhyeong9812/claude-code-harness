# 요구사항 명세서 — lazymode fail 재질문 자기완결 패킷 규칙

## 1. 목표·대상
`playbooks/implementation-lazymode.md` §4에 fail 재질문 형식 규칙 1개 항목 추가(+2줄). 실측 불편 2건 반영: (a) 재질문이 부족점+파일:줄만 줘서 사용자가 대화를 오가며 조립(2026-09-07, spring-framework-fork PR #37014 게이트) (b) 힌트가 호출 체인 구조만 주고 각 메서드 내부 분기를 안 보여줌(2026-09-03, J6 게이트).

## 2. 경계·불변식
기존 게이트 절차(x/2, 정답 비노출, 판정 워커 위임)는 불변 — 메인이 사용자에게 보내는 재질문의 **형식**만 규정. 파일 ≤80줄 유지(54->56).

## 3. 기준소스
사용자 지시 원문 2건(measurement 대상 세션 로그), 현행 §4.

## 4. 금지영역
다른 플레이북·훅 변경 금지. 배포는 hooks/deploy.sh 경유만.

## 5. 검증
diff self-review + 배포 후 ~/.claude/playbooks/ 사본 diff 0 확인(스모크).

## 6. stakes
낮음(문서 2줄, 절차 규칙 — 훅 무관). 모드 auto(사용자 지시 명확, 문구는 CLI 제시).

## 승인 상태
2026-09-07 사용자 지시 3회("하네스쪽 정의를 수정", "claude-code-harness쪽을 그렇게 수정", "lazy모드 불편은 하네스 개선이 맞다")로 방향 합의. 문구는 적용 diff로 제시.
