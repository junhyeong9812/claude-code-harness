# 요구사항 명세서 (requirement-spec)

> 작성일: YYYY-MM-DD · 작업 폴더: `docs/plans/YYYY-MM-DD/작업명/`
> **모든 L1 작업의 진입 게이트** — 전수 인터뷰(질문 많아도 좋음)의 답변 원문을 바탕으로 작성하고, **사용자 합의 시 훅이 SPEC=1을 기록**한다(core v4 §게이트). 아래 필수 6칸 중 **한 칸이라도 비면 아직 L0** — 빈 섹션 금지.
> 자명한 작업(오타 수준)은 각 칸 1행 축약 가능 — 칸 자체를 비우지는 않는다.

---

## 0. 요구사항 원문 (인터뷰 기록)

> 사용자 요청 원문 + 전수 질문·답변 요약. 답변의 뉘앙스("~는 건드리지 마" 등)는 그대로 보존 — 이후 모든 판단의 입력.

- 원문:
- Q/A:

---

## 1. 목표·대상 (필수)

> 어느 프로젝트/경로/모듈에서, 무엇이 되면 끝인가 — 한 문장.

## 2. 경계·불변식 (필수)

> 무엇이 항상 참이어야 하나 (버그=깨진 불변식 / 기능=새 경계 / 리팩토링=보존할 동작).

## 3. 기준소스 (필수)

> 무엇이 정답인가 (DB·branch·reference — 사용자 지정 최우선).

## 4. 금지영역 (필수)

> 건드리면 안 되는 파일·기능·브랜치.

## 5. 검증 방법 (필수)

> 무엇으로 완료를 증명하나 (build·test·count·diff·실행 확인).

## 6. stakes (필수)

> 틀리면 얼마나 아픈가 — core v4 §stakes 표로 판정(손실×낯섦×모호성×불가역). 사용자 지정과 표가 다르면 충돌 보고(조용한 축소 금지).

- 판정: 낮음 / 중간 / 높음 — 근거 1줄:

---

## 7. 자율성 (모드 — stakes와 별개 축)

- [ ] auto (기본 권장 — 합의 후 Claude 자율 실행)
- [ ] lazy (매 diff 이해 게이트 — 학습·OSS 기여)

## 8. load-bearing 가정 (1~2개)

> 뒤 슬라이스가 얹힐 토대 가정 — **착수 직후 스모크로 조기 실증** 대상. 예측형 실패모드 나열은 하지 않는다(날조 방지 — 2개월 실측).

## 9. task 분해 (다단계일 때 — 독립 검증 가능 단위 2개+)

| task | 목표 | 의존 | acceptance |
|------|------|------|-----------|
| 01 | | — | |

---

## 승인 상태

- [ ] 필수 6칸 전부 기입 (빈 칸 없음)
- [ ] 사용자 합의 → SPEC=1 (훅 기록 — Claude 직접 편집 금지)
- [ ] 자율성 선택 → MODE 기록
