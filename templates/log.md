# log — 라이브 타임라인 + 리뷰 ledger

> 작업 폴더의 두 번째 파일(명세서와 한 쌍). **메인이 단일 writer** — 워커 산출은 packet 원시각을 명시해 메인이 append(미회수 시 "미회수" 행 필수).
> **발생 시점에 append** — 사후 재구성 금지. 긴급 경로에서는 이 파일 생성이 게이트 리셋 신호를 겸한다(core v4 §게이트 — 수정 착수 전 생성).

## 타임라인

> `시각 | 사건 | 결과/결정` 1~3줄 — 시도→실패→해결→결정, 테스트 단위 구분.

| 시각 | 사건 | 결과/결정 |
|------|------|----------|
| | | |

## 리뷰 ledger (中↑ — 듀얼 리뷰가 돈 작업 필수)

> 스키마 정본 = `playbooks/review.md §2`. 필드 삭제 금지 — 파일만 통합된 것.

| id | first_seen_loop | source | 근거(file:line) | disposition | status | fixed_in_loop |
|----|-----------------|--------|-----------------|-------------|--------|---------------|
| | | | | | | |

- verified(대칭 부담 — 신규 채택 0인 루프만): `lens / 근거 / how / source`

## 생략한 검증 (긴급 경로 빚 — DEBT=1인 동안 정본)

> 항목별로 기입, 해소 시 취소선+해소 확인 1줄. **전항 해소를 사용자 확인 후 set-state로 DEBT=0** — 그 전 "작업 완료" 선언 금지.

- [ ]

## 완료 요약 (마감 시)

> 무엇이 됐나 · 핵심 diff before/after 스니펫(**실파일에서 복사** — 메모리 재현·placeholder 금지, Opus 워커 위임) · 배운 것 · 남은 빚/이월.
