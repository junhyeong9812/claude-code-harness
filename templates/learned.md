# 학습 기록 (Learned)

> 작성일: YYYY-MM-DD
> 관련 산출물: (task.md 경로)
> 작업 요약: (한 줄 요약)

> **목적: 사용자의 학습.** 구현을 따라가지 못한 부분을 나중에 이 문서로 공부한다 — 프로세스 기록이 아니라 **제품 산출물**이다. 토큰을 아끼지 않는다.
> **트리거 (core §3.5)**: **코드 구현이 있는 작업마다 풀 작성**(2026-06-12 상시 승격 — 규모·stakes 무관). 문서-only 작업만 task.md 안 5줄 요약으로 대체.
> **⚠️ 작성 전 필수**: 이번 작업에서 생성/수정한 모든 파일을 **다시 열어서 읽는다.** 메모리에서 코드를 재현하지 않는다. 파일에서 직접 복사한다. 기대 수준은 `templates/learned-example.md`.

---

## 1. 사용된 라이브러리

| 라이브러리 | 버전 | 용도 | 왜 선택했는가 |
|-----------|------|------|-------------|
| | | | |

---

## 2. 핵심 함수 / 메서드

각 라이브러리에서 실제로 사용한 함수와 메서드를 전부 기록한다.

### 라이브러리명

| 함수/메서드 | 시그니처 | 역할 | 사용 위치 |
|------------|---------|------|----------|
| | | | |

> **⚠️ 아래 코드 블록에 실제 코드를 붙여넣는다. 빈 칸으로 두지 않는다.**
> 해당 파일을 Read 도구로 다시 열어서 코드를 확인하고 복사한다.

**사용 예시:**
```
(파일에서 직접 복사한 코드)
```
- 출처: `파일경로:라인번호`

**코드 설명:**
> 위 코드 스니펫에서 사용된 함수/메서드를 한 줄씩 설명한다.
> `함수명(인자)` — 역할 설명, 반환값, 왜 여기서 사용했는가

---

## 3. 어노테이션 / 데코레이터

| 어노테이션/데코레이터 | 소속 | 역할 | 적용 대상 |
|--------------------|------|------|----------|
| | | | |

**동작 원리:**
(각 어노테이션이 내부적으로 어떻게 동작하는지 간략 설명)

---

## 4. 수정 전/후 코드 비교

> **⚠️ 기존 파일을 수정한 경우**, 수정 전/후 코드를 나란히 기록한다.
> 신규 파일만 생성한 경우 이 섹션은 "해당 없음 (신규 파일만 생성)"으로 표기한다.

### 파일명: `파일경로`

**수정 전:**
```
(수정 전 코드 — git diff 또는 파일 히스토리에서 확인)
```

**수정 후:**
```
(수정 후 코드 — 현재 파일에서 직접 복사)
```

**변경 이유:** (왜 이렇게 바꿨는가)

**변경된 함수/메서드 설명:**
| 함수/메서드 | 변경 내용 | 이유 |
|------------|----------|------|
| | | |

---

## 5. 동작 구조

> 이번 작업에서 구현/수정한 기능의 **실행 흐름**을 기록한다.
> 요청이 들어왔을 때 어떤 순서로 어떤 컴포넌트를 거치는지 전체 흐름을 보여준다.

### 실행 흐름

```
(요청 → 처리 흐름을 텍스트 다이어그램으로 표현)

예시:
Client Request
  → Controller (입력 검증)
    → Service (비즈니스 로직)
      → Repository (DB 조회)
    ← Service (결과 가공)
  ← Controller (응답 반환)
Client Response
```

### 컴포넌트별 역할

| 컴포넌트 | 파일 | 역할 | 호출하는 메서드 |
|----------|------|------|---------------|
| | | | |

### 데이터 흐름

```
(데이터가 어떤 형태로 변환되며 흘러가는지)

예시:
LoginRequest(DTO)
  → Service: email로 User(Entity) 조회
  → Service: password 검증
  → JwtProvider: Claims 생성 → JWT 문자열
  → LoginResponse(DTO): accessToken + refreshToken
```

---

## 6. 디자인 패턴

| 패턴 | 적용 위치 | 왜 사용했는가 | 구조 |
|------|----------|-------------|------|
| | | | |

**패턴 상세:**

### 패턴명
- **의도**: 이 패턴이 해결하는 문제
- **구조**: 참여 객체와 관계
- **이 프로젝트에서의 적용**:

> **⚠️ 아래에 실제 적용 코드를 붙여넣는다.**

```
(파일에서 직접 복사한 코드)
```
- 출처: `파일경로:라인번호`

---

## 7. 설정 / 컨벤션

| 항목 | 값 | 이유 |
|------|---|------|
| | | |

---

## 8. 테스트에서 사용된 것들

### 테스트 프레임워크

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| | | |

### 테스트 유틸리티 / 헬퍼

| 함수/클래스 | 소속 | 역할 | 사용 예시 |
|------------|------|------|----------|
| | | | |

### Mock / Stub / Spy

| 도구 | 사용 방식 | 대상 | 왜 mock했는가 |
|------|----------|------|-------------|
| | | | |

### 테스트 어노테이션 / 데코레이터

| 어노테이션 | 소속 | 역할 |
|-----------|------|------|
| | | |

### Assertion 메서드

| 메서드 | 소속 | 검증 내용 | 예시 |
|--------|------|----------|------|
| | | | |

### 픽스처 / 팩토리

| 이름 | 유형 | 생성 대상 | 사용 위치 |
|------|------|----------|----------|
| | | | |

> **⚠️ 아래에 대표 테스트 코드를 붙여넣는다.**

**대표 테스트 코드:**
```
(테스트 파일에서 직접 복사한 코드)
```
- 출처: `파일경로:라인번호`

---

## 9. 새로 알게 된 것

(이번 작업을 통해 새로 배우거나 깨달은 점을 자유롭게 기록)

-

---

## 10. 더 공부할 것

(이번 작업 중 깊이 이해하지 못했거나, 나중에 더 파봐야 할 주제)

| 주제 | 왜 공부해야 하는가 | 참고 자료 |
|------|-----------------|----------|
| | | |

---
