# Claude Code 에이전트 오케스트레이션 — 시스템 철학

> AI한테 그냥 시키지 말고, 공장처럼 체계를 만들어서 실수 없이 깔끔하게 뽑아내자.

> 이 문서는 시스템의 **철학과 원칙**을 설명한다. 실제 운용 규칙은 `orchestration*.md` 4개 파일에 있다.

---

## 0. 왜 이 시스템이 필요한가

### AI의 근본적 한계: 기억 리셋

인테리어 업체에 비유하면 이렇다:

1. 상세한 지시를 내린다 ("거실은 이렇게, 부엌은 저렇게")
2. AI가 열심히 작업을 시작한다
3. 30분이 경과한다
4. **기억 리셋!** — "저희가 뭐 하기로 했죠?"

이것이 30분마다 무한 반복된다. 이게 AI한테 실제로 일어나는 일이다.

**해결책:** AI에게 외부 기억장치(산출물 문서)를 만들어주고, 매 단계마다 그 문서를 읽고 따르도록 강제하는 시스템이 필요하다.

---

## 1. 철학 — 사람 관리와 AI 관리는 똑같다

사람에게 일을 잘 시키려면 3가지가 필요하다:

| 사람 관리 | AI 관리 |
|---------|--------|
| 업무 매뉴얼 | → 오케스트레이션 문서 (`orchestration*.md`) |
| 체크리스트 | → 산출물 (`checklist.md`, 단계별 보안 체크리스트) |
| 보고 체계 | → 필수 게이트 (진단보고/이해확인/목표정의에서 사용자 승인 강제) |

이 3가지를 AI한테도 그대로 적용하면 에이스 직원처럼 쓸 수 있다.

---

## 2. 모드 분리 — 작업 성격이 다르면 흐름도 달라야 한다

**v1의 문제**: 단일 오케스트레이션이 모든 작업에 5단계 파이프라인을 강제 → 토론·학습 중에도 "리서치 → 계획 → 구현…"이 강제되어 부자연스러웠다.

**v2의 해결**: 라우터(`orchestration.md`)가 모드를 판단하고 분기한다.

```
구현 모드  → orchestration-impl.md  (서브 파이프라인 3종: 버그/기능/리팩토링)
토론 모드  → orchestration-discuss.md (자유 대화 흐름)
에이전트   → orchestration-agent.md  (서브 에이전트 운용 가이드)
```

---

## 3. 서브 파이프라인 — 작업 유형별 핵심 위험이 다르다

구현 모드 안에서도 작업 유형마다 가장 위험한 실패 양상이 다르다. 그래서 흐름이 갈라진다.

| 유형 | 핵심 위험 | 흐름의 강조점 |
|------|----------|--------------|
| 버그 수정 | **오진** — 원인 잘못 짚고 수정 | [조사] → [진단보고: 사용자 승인 필수] → 수정 |
| 기능 구현 | **방향 오류** — 잘못된 방향으로 빠르게 달림 | [리서치] → [이해확인: 사용자 승인 필수] → 계획 |
| 리팩토링 | **기존 기능 파괴** — 동작이 바뀜 | [현상분석] → [목표정의: 동작 불변 약속] → 단계적 변환 |

각 파이프라인의 진단보고/이해확인/목표정의는 **필수 게이트**다. 게이트를 건너뛰는 즉시 시스템이 실패한다.

---

## 4. 산출물 4종 — AI의 외부 기억장치

모든 구현 작업은 규모와 무관하게 4개 문서를 남긴다.

| 문서 | 내용 | 비유 |
|------|------|------|
| **plan.md** | 변경 대상 + 금지 영역 + 트레이드오프 + 구현 순서 | 설계도 |
| **context.md** | 결정 근거 + 시스템 구조 + 주의사항 | 시방서 |
| **checklist.md** | 단계별 체크리스트 + 수정 기록 | 공정표 |
| **learned.md** | 사용한 라이브러리/함수/패턴/테스트 전부 | 작업 일지 |

저장 경로: `docs/plans/YYYY-MM-DD/작업명/`

이 폴더가 곧 **이 작업의 진실 공급원**이다. 다음 세션의 AI는 이 폴더를 읽고 그대로 이어받는다.

---

## 5. 패턴 참조 우선순위 — 일반론은 외부 문서로 두지 않는다

과거에는 도메인×스택×규모별 스킬 문서(45개, 41,000줄)를 두었다. 폐기했다.

이유:
- 내용의 90%가 LLM이 이미 아는 일반론(Spring Entity, React Hook, K8s manifest 등). 외부 문서로 다시 적어둘 가치 없음.
- 진짜 가치 있는 5%(사용자 본인의 편향)는 분리하면 너무 작아서 별도 시스템이 필요 없음.
- "필수 참조"라고 강제하면서도 "기존 코드 패턴이 우선"이라고 한 자기모순 → 결과적으로 dead code.

현재의 우선순위는 단순하다:

1. **`docs/guide/`** — 프로젝트 가이드가 있으면 최우선.
2. **기존 코드 컨벤션** — 가이드가 없으면 일관성 우선.
3. **LLM 일반 지식** — 위 둘 다 없으면 사용자에게 선택지를 제시하고 결정받는다.

---

## 6. 메모(주석)의 역할

코드나 계획에 달리는 메모는 4가지 기능을 한다:

- **가정 수정** — AI가 잘못 가정한 것을 바로잡기
- **접근 방식 거부** — AI의 제안을 거절하고 다른 방향 제시
- **제약조건 추가** — "이것만은 꼭 지켜라" 조건 부여
- **도메인 지식 전달** — AI가 모르는 업무 맥락 주입

대규모 작업에서는 사용자가 plan.md에 직접 주석을 달고, AI가 반영하는 사이클을 반복한다.

---

## 7. 방향 조정 패턴

AI에게 모든 걸 맡기는 게 아니라, 사람이 방향을 잡아준다:

1. **좋은 건 취하고 나머진 버리기** — 선택적 수용
2. **과감하게 쳐내기** — 불필요한 추상화 제거
3. **건드리면 안 되는 선 긋기** — 금지 영역 명시 (`plan.md`의 "변경 금지 영역")
4. **기술 선택은 사람 몫** — 프레임워크/라이브러리 선택은 AI가 결정하지 않는다

---

## 8. 훅 시스템 — 자동 매뉴얼 전달

훅은 클로드 코드의 **자동 감지 + 자동 알림** 장치다. 매 프롬프트가 들어올 때마다, 현재 모드/단계에 맞는 리마인더를 자동으로 컨텍스트에 주입한다.

```
사용자 입력 → UserPromptSubmit 훅 → 모드/단계 리마인더 표시 → AI 작업
```

### 훅이 하는 일

| 훅 | 동작 |
|----|------|
| `prompt-guard.sh` | 매 프롬프트마다 모드별 리마인더 표시 (구현: 현재 단계 + "코드를 추론하지 마세요" / 토론: 모드 표시) |
| `stage-transition.sh` | 모드/단계 전환 유틸리티 (`discuss`, `1`~`5`) |

> 강제로 막는 게 아니라, "지금 이 단계야, 이거 잊지 마" 하고 상기시키는 것이다.

---

## 9. 셀프체크 — 완료 후 자동 점검

피드백 단계에서 AI가 자기 작업을 점검한다. `orchestration-impl.md` §11의 체크리스트:

- 요구사항을 빠짐없이 구현했는가?
- 계획에 없는 변경을 하지 않았는가?
- `plan.md`의 "변경 대상 파일" 외의 파일을 수정하지 않았는가?
- 기존 기능을 깨뜨리지 않았는가?
- (보안 체크리스트 — 단계별로 인라인)

---

## 10. 전체 흐름 — 9단계

```
1. 사용자 지시            — 요구사항 전달
2. 모드 판단              — 라우터가 구현/토론/에이전트로 분기
3. (구현 모드) 유형 판단   — 버그/기능/리팩토링 → 서브 파이프라인 진입
4. 리서치/조사            — 코드를 실제로 읽는다 (추론 금지)
5. 필수 게이트            — 진단보고 / 이해확인 / 목표정의 → 사용자 승인
6. 계획 산출물 작성        — plan/context/checklist 3종
7. 구현                  — 범위 통제 + 금지 영역 준수
8. 테스트                — 회귀 + 신규 기능
9. 피드백                — 셀프체크 + learned.md + project-overview 업데이트
```

5단계의 게이트가 가장 중요하다. 여기서 멈추지 않으면 시스템이 실패한다.

---

## 11. 적용 전 vs 후

| 적용 전 ❌ | 적용 후 ✅ |
|---------|---------|
| 매뉴얼 적어놔도 AI가 무시 | 정해진 흐름 자동 적용 |
| 매번 수동으로 알려줘야 함 | 시작 전 알아서 확인 |
| 결과물 품질 들쑥날쑥 | 결과물 품질 일정 유지 |
| 창의적 해석 고치느라 낭비 | 수정 시간 대폭 감소 |
| 다음 세션에서 맥락 사라짐 | `docs/plans/` 가 외부 기억으로 남음 |

---

## 최종 핵심 정리

| # | 시스템 | 설명 |
|---|--------|------|
| 1 | **모드 라우터** | 작업 성격에 맞는 흐름으로 자동 분기 |
| 2 | **서브 파이프라인** | 유형별(버그/기능/리팩토링) 핵심 위험에 맞춘 흐름 |
| 3 | **필수 게이트** | 진단/이해/목표 단계에서 사용자 승인 강제 |
| 4 | **산출물 4종** | plan/context/checklist/learned — AI의 외부 기억 |
| 5 | **훅** | 매 프롬프트마다 현재 단계 자동 리마인드 |
| 6 | **셀프체크** | 완료 후 자기 작업 점검 + 보안 체크리스트 |

---

## 도입 이후 결과

**AI가 실수를 남기고 넘어간 적이 단 한 번도 없다.**

게이트가 바로 잡아내고, 산출물이 바로 다음 세션으로 인계된다.
