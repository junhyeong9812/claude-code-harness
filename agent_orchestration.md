# Claude Code 에이전트 오케스트레이션 완전 가이드

> AI한테 그냥 시키지 말고, 공장처럼 체계를 만들어서 실수 없이 깔끔하게 뽑아내자.

---

## 0. 왜 이 시스템이 필요한가

### AI의 근본적 한계: 기억 리셋

인테리어 업체에 비유하면 이렇다:

1. 상세한 지시를 내린다 ("거실은 이렇게, 부엌은 저렇게")
2. AI가 열심히 작업을 시작한다
3. 30분이 경과한다
4. **기억 리셋!** — "저희가 뭐 하기로 했죠?"

이것이 30분마다 무한 반복된다. 이게 AI한테 실제로 일어나는 일이다.

**해결책:** AI에게 외부 기억장치를 만들어주고, 자동으로 매뉴얼을 참고하게 만드는 시스템이 필요하다.

---

## 1단계. 철학 — 사람 관리와 AI 관리는 똑같다

사람에게 일을 잘 시키려면 3가지가 필요하다:

| 사람 관리 | AI 관리 |
|---------|--------|
| 업무 매뉴얼 | → AI가 참고하도록 |
| 체크리스트 | → AI가 검증하도록 |
| 보고 체계 | → AI가 확인하도록 |

이 3가지를 AI한테도 그대로 적용하면 에이스 직원처럼 쓸 수 있다.

---

## 2단계. 작업 파이프라인 — 5단계 흐름

전체 작업은 항상 이 순서를 따른다:

```
코드리서치 → 계획 → 주석달기(반복) → 구현 → 피드백
```

### 4단계 방어 체계

각 단계가 방어막 역할을 한다:

| 단계 | 역할 | 효과 |
|-----|------|------|
| 1. 리서치 | 코드베이스를 깊이 읽는다 | 무지한 변경을 막는다 |
| 2. 계획 | 계획을 작성한다 | 잘못된 변경을 막는다 |
| 3. 주석 달기 | 내 판단을 주입한다 | AI의 창의적 해석을 통제한다 |
| 4. 구현 | 명령을 실행한다 | 방해 없이 계획대로 진행한다 |

### 핵심 원칙: "아직 구현하지 마"

이 문구를 **명시적으로** 써줘야 한다. 주석 달기 사이클에서 창의적인 작업과 진짜 중요한 판단이 끝나고, 구현 단계에서는 그냥 계획대로 찍어내기만 하면 된다.

---

## 3단계. 계획 수립 — 3단계 프로세스

### 3-1. AI에게 계획 수립 시키기

현재 상황을 분석하고 계획을 작성하게 한다.

### 3-2. 꼼꼼히 검토하기

AI의 해석 오류를 방지한다. **시간을 들여 확인**한다.

### 3-3. 문서로 저장하기

3가지 파일로 저장한다 → 이것이 AI의 외부 기억장치가 된다.

| 문서 | 내용 | 건축 비유 |
|-----|------|---------|
| **계획서** | 뭘 만들 건지, 처음부터 끝까지 | 설계도 |
| **맥락 노트** | 왜 이렇게 결정했는지, 관련 자료 위치 | 시방서 |
| **체크리스트** | 뭘 끝냈고 뭐가 남았는지 추적 | 공정표 |

---

## 4단계. plan.md 중심 워크플로우

### 내장 플랜 모드 vs 자체 .md 파일

| 내장 플랜 모드 | 자체 .MD 파일 |
|-------------|-------------|
| 채팅창에 흘러가버림 | 에디터에서 편집 가능 |
| 수정 불가 | 인라인 메모 가능 |
| 휘발성 | 실제 산출물로 남음 |

### plan.md에 담기는 것들

1. **접근 방식 상세 설명** — 어떻게 할 건지
2. **코드 스니펫** — 참고할 코드 조각
3. **파일 경로** — 어떤 파일을 건드릴 건지
4. **트레이드오프** — 무엇을 취하고 무엇을 포기하는지

### plan.md 워크플로우

```
plan.md 작성 → 에디터에서 검토 → 주석 메모 추가 → Claude에게 전달 → 계획 업데이트
```

---

## 5단계. 방향 조정 패턴

AI에게 모든 걸 맡기는 게 아니라, 사람이 방향을 잡아준다:

1. **좋은 건 취하고 나머진 버리기** — 선택적 수용
2. **과감하게 쳐내기** — 불필요한 것 제거
3. **건드리면 안 되는 선 긋기** — 금지 영역 명시
4. **기술 선택은 내가 하기** — 핵심 결정은 사람 몫

---

## 6단계. 메모(주석)의 역할

코드나 계획에 달리는 메모는 4가지 기능을 한다:

- **가정 수정** — AI가 잘못 가정한 것을 바로잡기
- **접근 방식 거부** — AI의 제안을 거절하고 다른 방향 제시
- **제약조건 추가** — "이것만은 꼭 지켜라" 조건 부여
- **도메인 지식 전달** — AI가 모르는 업무 맥락 주입

---

## 7단계. 구조 개선 — 거대 파일 쪼개기

1,500줄짜리 거대한 매뉴얼 파일 하나를 아래처럼 분리한다:

```
기존: 1,500줄 거대한 파일 하나

개선: 목차 (INDEX)
      ├── 챕터 1
      ├── 챕터 2
      └── 챕터 3 ...
```

AI가 **목차만 먼저 보고**, 지금 작업할 챕터만 열어보는 구조로 만든다.

---

## 8단계. 클로드 코드 훅(Hook) 시스템 — 자동 매뉴얼 시스템

### 훅이란?

클로드 코드의 자동 감지 + 자동 알림 장치다.

```
명령 입력 → 훅 감지(자동으로) → 매뉴얼 전달(강제로) → AI 실행(올바르게)
```

### 첫 번째 장치: 시작 전 알림 (UserPromptSubmit 훅)

사용자가 지시를 입력하면:

1. **UserPromptSubmit 훅**이 지시 내용을 분석
2. 관련 **스킬 파일을 자동 선택** (예: `.claude/skills/backend-skill.md`)
3. AI가 스킬 가이드를 참고해서 **올바른 방식으로 작업 시작**

### 자동 매칭 시스템: 4가지 매칭 조건

매뉴얼마다 아래 4가지 조건을 정해놓는다:

| 조건 | 감지 대상 | 예시 |
|-----|---------|------|
| **키워드** | 특정 단어 | "백엔드", "서버", "API" → 백엔드 스킬 활성화 |
| **의도 파악** | 요청 패턴 | "만들어줘", "추가해줘" → 해당 영역 스킬 활성화 |
| **작업 위치** | 파일 경로 | `src/api/` 폴더 수정 중 → 백엔드 스킬 자동 활성화 |
| **파일 내용** | 코드 패턴 | `import express` 패턴 → 관련 스킬 자동 활성화 |

### 두 번째 장치: 완료 후 체크 (PostToolUse 훅)

AI가 작업을 완료하면:

1. **PostToolUse 훅**이 결과물을 분석
2. 결과물을 검토 — "혹시 빠뜨린 부분은 없나?"
3. 체크 질문 전달:
   - "혹시 위험한 부분은 없나요?"
   - "에러 처리는 추가했나요?"
   - "이 부분 보안은 확인했나요?"

> 강제로 막는 게 아니라, "혹시 이것도 확인했어?" 하고 상기시키는 것이다.

---

## 9단계. 3가지 자동 안전장치

### 9-1. 수정기록장치

CCTV처럼 어떤 파일을 고쳤는지 타임스탬프와 함께 자동 기록한다.

```
수정 기록 로그:
• auth.service.ts    14:23:01
• api.routes.ts      14:23:08
• db.schema.ts       14:23:15
• user.model.ts      14:23:22
• config.env.ts      14:23:29
```

### 9-2. 완료 후 검사장치

AI가 대답을 완전히 끝냈을 때 자동으로 검사를 돌린다.

```
AI 완료 → 자동 검사
```

### 9-3. 자동품질검사

공장 컨베이어 벨트처럼, AI 산출물도 자동 검사가 필요하다.

---

## 10단계. 셀프체크 리마인더

작업 완료 후 AI에게 자동으로 확인 질문을 던진다:

```
방금 수정한 파일 2개에서 확인할 것:
✅ 혹시 오류 처리는 추가했나요?
✅ 보안상 위험한 부분은 없나요?

→ 막는 게 아니라, 살짝 확인해 주는 거예요
```

---

## 11단계. 전체 자동 검사 흐름 — 7단계

```
1. 지시 내리기        — 명령 전달
2. 매뉴얼 자동 전달    — 관련 컨텍스트 로딩
3. AI 작업 수행       — 코드 생성 & 수정
4. 파일 자동 기록      — 변경 로그 저장
5. 오류 자동 검사      — 린트 & 타입 체크 ★
6. 셀프체크 리마인더   — "혹시 이것도 확인했어?" ★
7. AI 바로 수정       — 자동 보완 완료 ★
```

> 5~7단계가 핵심이다. 오류가 있으면 AI가 바로 수정한다.

---

## 12단계. 전문 AI 팀 구조

역할별 전문 AI 팀원을 배치한다:

| 팀 | 역할 |
|---|------|
| **품질관리팀** | 코드 검토, 오류 수정, 구조 개선 |
| **테스트팀** | 기능 테스트, 오류 진단, 화면 확인 |
| **기획팀** | 계획 수립, 계획 검토, 문서 작성 |

---

## 13단계. 공장 품질관리 비유 — 전체 시스템

```
원자재 투입 → 생산 라인 → 자동 검수 → 불량 재작업 → 출고 완료
(스펙 확인)  (AI 작업)   (오류 검사)  (AI 바로 수정) (실수 없는 결과물)
```

> 잔재 없이 깔끔하게. 이게 이 시스템의 모토다.

---

## 14단계. 핵심 팁 — 계획 승인 직후 워크플로우

계획을 승인한 직후 바로 이 흐름을 따른다:

```
계획 승인 ✓ → 멈춤! → 3개 문서 생성 → 새 대화 시작
(마음에 들면) (바로 달려들지 못하게) (계획·맥락·체크리스트) (저장된 문서 읽고 이어서)
```

> **"저장된 문서 읽고 이어서 작업해"** — 이 한마디면 된다.

---

## 15단계. 시스템 적용 전 vs 후

| 적용 전 ❌ | 적용 후 ✅ |
|---------|---------|
| 매뉴얼 적어놔도 AI가 무시 | 정해진 방식 자동 적용 |
| 매번 수동으로 알려줘야 함 | 시작 전 알아서 매뉴얼 확인 |
| 결과물 품질 들쑥날쑥 | 결과물 품질 일정 유지 |
| 창의적 해석 고치느라 낭비 | 수정 시간 대폭 감소 |

---

## 최종 핵심 정리

| # | 시스템 | 설명 |
|---|--------|------|
| 1 | **자동 매뉴얼 시스템** | 키워드·의도·위치 → 매뉴얼 자동 활성화 |
| 2 | **작업 기억 시스템** | 계획서·맥락 노트·체크리스트 |
| 3 | **자동 품질 검사** | 완료 시 오류 자동 체크 → 즉시 수정 |
| 4 | **전문 에이전트** | 역할별 전문 AI 팀원을 배치하고 사용 |

---

## 도입 이후 결과

**AI가 실수를 남기고 넘어간 적이 단 한 번도 없다.**

검사가 바로 잡아내고, AI가 즉시 고치기 때문이다.
