# 정의 (Definition)

> 작성일: YYYY-MM-DD
> 위치: B2(방향성) 통과 후 **B2.5 정의 게이트**에서 작성. 계획(B3) 직전.
> 본 문서는 "이 기능이 닿는 각 경계에서 **무엇이 항상 참이어야 하고(불변식), 실패하면 어떻게 되는가(실패 의미론)**"를 못 박는다.
> 근거: `docs/09-AI시대-개발의-본질-정리.md`(개발=경계의 불변식·계약·실패의미론 정의) → `docs/10-정의-게이트-설계안.md`(B2.5).

> **⚠️ 규모별 강도** (impl 1.2 / B2.5 — 과설계 금지):
> - **소**(1~2파일, 위험영역 아님): 이 문서 대신 plan/경량기록에 `불변식 / 실패 시 동작: 1~3줄 또는 N/A`
> - **중**(3~10파일): 변경 *경계별* 계약·실패의미론을 짧게 (이 문서 경량 또는 plan 내 "정의" 섹션)
> - **대 / 위험 승격**(데이터·동시성·외부 I/O·마이그레이션·인증·결제·DB스키마): 이 문서 **풀 작성 필수** + codex 발굴

---

## 0. 작업 정보

| 항목 | 내용 |
|------|------|
| 작업명 | |
| 규모 / scale_decider | |
| 관련 research.md | |
| 브리지 출처 (discuss definition.md 있으면) | |

---

## 1. 경계 목록 (Boundaries)

> 이 기능이 닿는 모든 경계. 안에서 닫힌 로직이 아니라 **넘나드는 지점**을 적는다.

| # | 경계 | 종류 (API · DB tx · 서비스간 · 이벤트 · 외부 I/O) | 설명 |
|---|------|------|------|
| 1 | | | |

---

## 2. 경계별 불변식 (Invariants — 항상 참)

> acceptance("되면 OK")가 아니라 **어떤 경우에도 깨지면 안 되는 것**.

| 경계# | 불변식 | 깨지면 발생하는 일 |
|-------|--------|------------------|
| | | |

---

## 3. 경계별 계약 (Contracts)

| 경계# | 입력 (형식·범위) | 출력 (형식·범위) | precondition | postcondition |
|-------|------|------|------|------|
| | | | | |

---

## 4. 실패 의미론 (Failure Semantics)

| 경계# | 재시도 가능? | 중복 처리 (idempotency 단위) | 부분 성공 시 | resume 기준 | 사용자에게 보이는 상태 | rollback vs forward-fix | consistency boundary |
|-------|------|------|------|------|------|------|------|
| | | | | | | | |

---

## 5. 빠뜨리기 쉬운 4축

> 각 "해당 없음" 명시 가능. **빈칸 금지** (codex 보강 — 누락 방지).

| 축 | 해당 여부 | 내용 |
|----|----------|------|
| 관찰가능성 (로그/메트릭/추적) | | |
| 보안·권한 경계 | | |
| 동시성 | | |
| 시간·순서 의존성 | | |

---

## 6. 외부 경계 최소 체크 (외부 I/O 있을 때만)

| 항목 | 해당 여부 | 대응 |
|------|----------|------|
| rate limit | | |
| timeout | | |
| stale read | | |
| schema drift | | |
| 감사 로그 | | |

---

## 7. 애매성 해소 (★ 가드레일 — 미결정 0이어야 B3 진입 ★)

> open question을 **결정으로 전환**. 결정이 필요하면 `AskUserQuestion`으로 사용자 결정 수신.

| # | 애매했던 질문 | 결정 | 결정 주체 (사용자/근거) |
|---|------|------|------|
| | | | |

- **미결정(open) 항목 수: ___ (0이어야 B2.5 통과)**

---

## 8. 검증 매핑 (불변식 → 증명 방법)

> 각 불변식을 어떤 테스트/검증명령으로 증명하나. **X4-T(6.7) 테스트 설계의 입력**이 된다.
> 데이터 작업이면 **6.6 특칙**(source/target count·orphan·sample diff)이 이 검증 매핑의 구현이다.

| 불변식 | 검증 방법 (테스트/명령/쿼리) | X4-T 반영 |
|--------|------|------|
| | | |

---

## 9. domain-contracts.md 승격 후보

> 승격 기준 (셋 중 하나 이상): ① 2회 이상 재사용 ② 깨지면 장애/데이터 손상 ③ 테스트 기준으로 쓰임.
> 피드백(B6) 단계에서 충족분을 `docs/domain-contracts.md`로 승격(전수화 금지).

| 불변식/계약 | 승격 기준 충족 | 비고 |
|------|------|------|
| | | |

---

## 10. codex 발굴 기록 (대/고위험)

- 호출 ID: (예: `작업명/B2.5/001`)
- 입력 해시 (sha256 앞 12자):
- 호출 시각 / 종료 코드:

codex가 추가 발굴한 경계/불변식/실패모드:

| # | codex 지적 | 채택 / 기각 |
|---|------|------|
| | | |

- 호출 실패 시: 자동 스킵 + 사유·시각 기록.

---

## 변경 이력

| 시각 | 변경 내용 |
|------|----------|
| | |
