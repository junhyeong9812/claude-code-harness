# task: changelog.md 학습 산출물 도입 (코드 구현 작업마다)

## 1. 정의 (명확도 6칸)

| 칸 | 내용 |
|----|------|
| 목표·대상 | 이 repo의 `core.md`·`templates/`에서, 코드 구현이 있는 모든 작업이 작업 폴더에 `changelog.md`(구현 근거 + 실코드 스니펫 + 근거 주석 + 리뷰 연습 포인트)를 산출하도록 규칙·템플릿이 배선되면 끝 |
| 경계·불변식 | ① 단일 출처 — 형식은 templates/changelog.md만 소유, core는 트리거·포인터 ② learned.md와 역할 분리 명확(changelog=구현 근거·매 코드 작업 / learned=새 지식·학습 가치 트리거) ③ 문서 인용 규칙(실파일 복사·placeholder 금지) 적용 ④ 문서-only 작업엔 만들지 않는다 |
| 기준소스 | 사용자 결정(2026-06-12): 목적=사용자의 코드 리뷰 능력 훈련 / 트리거=코드 구현 작업만 / 범위=판단 개입 변경 전부(기계적 변경은 1줄) / 내용=근거+스니펫+주석 |
| 금지영역 | dimensions*.md, hooks/, archive/, 기존 learned.md 규칙 약화 |
| 검증 방법 | 템플릿 ≤80줄 / core 배선 grep(§3.5·§7) / codex 최종 검증 1회 (문서 작업 — 리뷰 루프 비대상, core §5) |
| stakes | **높음** — 하네스/정책 변경 (core §2) |

### 트리아지: 런타임 차원 전부 비활성(문서만 변경) · #14 도메인 규칙 활성(규칙 정합·단일 출처가 실패모드) → 차원상 낮음이나 core §2 하한으로 높음.

## 2. 계획

- 변경 파일:
  1. `templates/changelog.md` **신설** — 변경 단위별 항목(무엇/왜·근거/스니펫+주석/대안 기각/리뷰 연습 포인트 — review.md 렌즈 연결) + 기계적 변경 1줄 목록
  2. `core.md` — §3.5 기록 절에 changelog 산출 의무 추가(코드 구현 작업, 검증 통과 후 작성), §7 표에 templates/changelog.md 행, 변경 이력 1행
- 변경하지 않을 파일: dimensions*.md, hooks/, playbooks/(필요 시 포인터 1줄만), 기존 learned 규칙
- 순서: codex 계획 검토 → 반영 → 템플릿 작성 → core 배선 → 검증 → codex 최종 검증 → 커밋(규칙/docs 분리)·push·~/.claude 동기
- 검증 명령: `wc -l templates/changelog.md` / `grep -n changelog core.md templates/ playbooks/ -r`

## 3. 진행 기록

- 2026-06-12: 사용자 결정 2건(트리거=코드 구현만 / 스니펫=판단 개입 전부). codex 계획 검토 8지적 전부 채택 — 특히 ① 스니펫 내 주석 삽입은 인용 규칙 위반 → 원본 블록 + 라인별 근거 표로 변경 ② learned 경계 고정 ③ J/M/G 전수 분류 ④ 근거 출처 필드. 변경 파일에 templates/task.md 추가(지적 6).
- 사용자 추가 결정: **대안 비교 표** 신설 — 선택 전 존재한 방법들을 장단점과 함께 나열 (실검토만, 사후 창작 금지).
- codex 최종 검증 3지적 반영: 커버리지 diff 기준 명시+프로세스 산출물 제외(자기 분류 순환 방지) / learned 스니펫 소유권 문구 정합(core·task 템플릿) / "task.md 1파일" 문구를 트리거별 별도 산출물과 정합.

## 4. 검증 결과

- 최소 안전선: 테스트 N/A(문서) / diff self-review ☑ (core.md·templates 2종 신설·수정, 계획과 일치 — templates/task.md는 codex 지적으로 계획 갱신 후 포함) / rollback ☑ / contract 영향 ☑ (이후 모든 코드 작업의 산출물 의무 추가) / 반증 질문 ☑ (codex 최종 검증)
- 검증 명령: `wc -l templates/changelog.md` = 51 (≤80 통과) / grep 배선 확인 — 형식은 템플릿만 소유, core §3.5·§7·task 템플릿은 트리거·포인터 (단일 출처 통과)
- codex 2회: 계획 8지적 전부 채택 (`codex-output-plan.md`) / 최종 3지적 전부 채택 (`codex-output-final.md`) — 오탐 0 (최종 입력에 "발췌 밖 기정의 단정 금지" 단서 추가 효과)

## 5. 기록

- 측정 1행 기입 완료 ☑
- changelog 판정: 문서-only라 제외 ☑ (코드 구현 없음)
- learned 판정: 새 지식 없음 → 5줄 요약:
  - 변경: 코드 구현 작업마다 changelog.md(의사결정 로그 — 대안 비교·근거 출처·라인별 해설·리뷰 연습 포인트) 산출 의무화
  - 검증: codex 2회(계획 8·최종 3) 전부 채택·반영
  - 새로 안 것: "작성 후 셀프체크" 류 규칙은 자기 자신이 검사 대상에 들어가는 순환을 먼저 닫아야 한다
  - 지적받은 것: 스니펫에 주석을 섞으면 "실파일 복사" 검증이 불가능해진다 — 해설은 코드 밖 표로
  - 반복 금지: 산출물 추가 시 기존 "기본 1파일" 같은 총칙 문구와의 충돌을 grep으로 먼저 확인
